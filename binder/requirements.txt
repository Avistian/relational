# Binder build for running lab notebooks in the browser (mybinder.org).
# Mirrors ../requirements-labs.txt. Binder opens notebooks with CWD=labs/,
# so `relkit` (in labs/) imports directly and OpenML fetch works (Binder has network).
numpy>=1.26
pandas>=2.1
scikit-learn>=1.5
imbalanced-learn>=0.12
pyarrow>=15.0
ipykernel>=6.29
jupyterlab>=4.2
