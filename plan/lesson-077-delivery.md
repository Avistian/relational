# Lesson 077 delivery design

Curriculum scope: single-table ceiling, synthesis/experiment; no new architecture or assigned paper table. User requests full reproduction. Reproduce the entire finite paired-history experiment locally, including generator, split, representations, classifier fitting, metrics and exact collision bound. Keep the published RelBench claim separate and point to the existing complete historical L076 replay, without inventing a paper target for this original construction.

Question: which labels are recoverable from a specified information interface? Build oppositely ordered histories with identical count/sum/mean/max and own-row features. All train/validation/test partitions preserve complete pairs and disjoint entities. Prove the finite-support accuracy upper bound by collision classes; compare a visible trained stump on the flat representation and on a restored time-order feature. SQL independently reconstructs both features; test identity permutation, future and late-arrival interventions, arbitrary minority class counts, and absent collisions with unique IDs. Five independently generated/split seeds reproduce the full 1000-pair experiment. Three live TODOs: flat representation, exact bound, ordered feature.

Alternatives: essay-only would omit the curriculum experiment; training a new GNN would confound information access with optimization and move ahead of L078. Use the exact construction with a fair tabular restoration control. Extend discussion through count, identity, two-hop access, set-reduction limits, constraints and practical non-IID barriers; keep synthetic and real-world claims distinct.

Artifacts: prose-backed lesson, three interactive computation widgets plus reused L035 collision, four portable figures, student/solution notebooks, execution/verifier results, reference, reproducibility contract, manifest/gallery/glossary/dossier updates. Verify canonical-inline parity, mutation detection, complete solution execution, desktop/mobile controls and copied Pages links. No learner mastery or live deployment inferred.
