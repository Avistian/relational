# Lesson 098 design and delivery

The curriculum authorizes a three-type RelBench-shaped toy graph and native heterogeneous NeighborLoader training. Build on L096 typed FK routes and L097 target/evaluation discipline; prepare L099 controlled architecture comparison and L100 implementation defense.

Chosen design: visible two-layer relation-wise mean encoder, real native sampler, independent dense forward/gradient oracle, full-neighbor versus fanout 2/1 training, and per-query temporal isolation. A prose-only API tour would not meet the lab objective; an entire RelBench benchmark would replace the specified toy unit and require feature/task pipelines outside this lesson.

Deliverables: canonical prose and HTML, whole-computation SVG/PNG, reusable sampled-route widget, standalone student/solution notebooks with three live tasks, trained model experiment records, exact rerun commands, pinned source/archive and environment evidence, reference sheet and manifest navigation. Correctness: 32 graphs × three batch sizes, all seed logits and gradients; native temporal disjoint queries; nine complete fits. Publication and learner mastery remain separate.

## Delivered evidence

96/96 correctness configurations and 9/9 40-epoch fits passed, including saved checkpoint restoration. Fresh standalone solution replay exactly matches all records. Browser widths 1200/375, all nine cutoff/fanout combinations, keyboard access, pedagogy widgets, print visibility, portable figure and copied Pages staging passed; all 41 local lesson/reference links resolve. Local render inspection corrected an overlong SVG label. Live Colab, a fresh native environment build and deployment are NOT_CHECKED.
