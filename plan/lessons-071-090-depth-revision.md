L071-L090 revision: retain the current labs, paper contracts and result evidence; add one model-specific overview and a deliberate worked path to every lesson. Link each path to prerequisite and next lessons and exact primary readings. Reuse native assets, build portable notebook images, and keep code/output provenance unchanged. Check every target's rendered output, links, mobile/desktop layout and builder regeneration before committing and pushing main.

Implemented: 20 canonical reading walkthroughs (about 10,200 added words), 22 model-specific or protocol-specific editable SVG diagrams with PNG notebook exports, a shared reading map, and a reusable delivery helper integrated with the existing 20 builders. Existing interactions, exercises, training code and author measurements remain in place.

Regenerate the diagrams with `.venv/bin/python labs/_architecture_071_090.py`, then run the affected `labs/_build_lNNN.py` builder. Builders preserve saved execution only after whole ordered code-source equality. The shared helper embeds the new images into both notebook variants and renders their student preview. Earlier locally generated solutions that were already ignored remain local; all 20 student notebooks and the nine existing versioned solutions are updated for publication.

Validation: all 20 builders passed; 29 versioned notebooks retain code, execution counts and outputs identical to baseline a5654350d1573b22033a94fb1036a963a4a7a9a1. L071 and L086 representative regeneration is byte-identical. Ten worked numerical quantities checked. 879 local lesson links pass in the workspace and an exact copy stage from the Pages workflow. All 20 lessons pass browser checks at 1440px and 390px for page overflow, JavaScript errors and image loading. SVG text bounds checked; representative screenshots inspected. See reviews/lessons-071-090-depth-check.json.

No benchmark was rerun for this editorial revision. GIN search incompleteness, unrun full Cluster-GCN/SEAL targets, historical identity gaps and live-Colab boundaries are retained explicitly.
