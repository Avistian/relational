# Lesson 109 delivery · 2026-09-26

Approved combined scope delivered locally. Every one of 8,712 targets regenerated from the nine-table, 97,606-row pinned F1 database and matched to both the released task archive and unchanged source SQL. All ten selected Table 4 heuristic cells MATCH. Complete prediction vectors, regenerated tables, schema/contract and checksums retained.

Full query-cutoff graph census: 310 cutoffs, 4,030 FK relation comparisons and 2,790 node-table comparisons against independent SQLite queries. Separate synthetic evidence: 520 version-selector SQL queries, 390 graph SQL queries and five rejected broken implementations. Historical ingestion/revision data is unavailable in F1; no claim of real two-clock availability follows.

Standalone student and executed solution: 19 executed code cells, three load-bearing TODOs, four portable mechanism/result figures. Fresh archive downloads in a clean directory; full numeric reconstruction and one complete real graph snapshot. Source SQL/full graph census are additional repository checks, with exact commands.

Desktop/mobile, all 26 timeline states, fixed baseline, keyboard/reset, prediction and teach-back gates, no-JS/print, canonical code identity and deterministic rebuild checks PASS. Copied Pages staging validates 44 lesson/reference links and manifest navigation. Four rendered notebook figures and the mobile widget inspected.

Closest earlier visual: L104 illustrated occurrence/availability gating. L109 adds selected row versions, a corrected FK, historical parent values and a deletion that must win selection before graph construction. The same example is carried through diagram, interaction and executable code.

USD0 paid compute; USD10 ceiling retained. Selected reconstruction COMPLETE; full paper and historical execution identity NOT_ESTABLISHED. Raw-source database reconstruction and learned model columns NOT_RUN. Live Colab and deployment NOT_CHECKED. Learner PENDING_WRITTEN_DEFENSE.
