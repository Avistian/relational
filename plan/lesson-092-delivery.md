# Lesson 092 — Meta-paths and HAN

Follow year-3 lesson 092: define typed paths, build endpoint neighborhoods, implement node attention and semantic fusion; connect R-GCN and GAT to HGT and later atomic routes. Prepared materials do not establish learner mastery.

Design: paper-first explanation plus an explicit released-code lane. A toy-only lesson cannot meet the requested reproduction scope. A DGL-only implementation obscures the paper/release semantic-attention discrepancy. Therefore provide a visible modern port of Jhy1993/HAN, full ACM3025 and its fixed split, full 200-epoch ceiling/patience100, and the Table3 KNN probe (all four fractions × ten permutations). Paper-global and release-node-specific fusion are distinguished and exercised separately. Historical identity remains INCOMPARABLE because original seeds/backend and table provenance are missing.

Deliver canonical prose/HTML, portable student and executed solution notebooks, source/data hashes, full CLI, protocol ledger, reference, model architecture and arithmetic figures, interactive interventions, behavioral checks, manifest/gallery integration, and copied Pages/browser checks. No publication requested.

Freeze before execution: target ACM Table3 HAN macro/micro F1 at20/40/60/80 percent = .8940/.8922, .8979/.8964, .8951/.8933, .9063/.9054. Descriptive closeness tolerance =2 percentage points, never overrides protocol differences. Run one complete released training schedule and ten KNN repetitions per fraction; additional initialization runs are separately counted if executed. No selection on test scores.
