# Lesson 110 delivery contract

Approved: complete selected TGN-attn Wikipedia reproduction and a paired clean-state course experiment. This is the Q3 deliverable checkpoint; no new model family is introduced. No learner completion or publication is inferred.

## Teaching artifact map

- Source `lessons/content/0110-temporal-gnn-checkpoint.md`; built lesson `lessons/0110-temporal-gnn-checkpoint.html`.
- Canonical full implementation `labs/relkit/checkpoint_l110.py`; original encoder/data definitions retained exactly from the independently audited L102 implementation.
- Builder `labs/_build_l110.py`; student and executed solution notebooks; prepared notebook HTML.
- Four diagram pairs generated by `labs/_figures_l110.py`: complete computation, coherent state selection, timestamp ties, measured per-seed results.
- Native input widget `assets/temporal-checkpoint-viz.js`; baseline and changed grouping remain visible.
- Reference `reference/temporal-gnn-checkpoint.html`; manifest-driven course/notebook navigation; stable retrieval item `l110-atomic-temporal-state`.

## Scientific gates

Both arms need complete seeds 0–9. Selection uses validation only. Release retains its historical queue/final-epoch policy; clean restores the complete best state and groups ties. The two arms have the same initial-seed list but not guaranteed identical candidate arrays or RNG trajectories. Numerical CLOSE applies only to the release mean and uses the predeclared 0.5 pp tolerance. Clean-state scores are a combined-policy intervention.

Checks are layered: independently reconstructed raw preprocessing/splits; original-source output/gradient/state equivalence; complete batch-frontier census; actual feature-mutation counterexamples; notebook task mutants; source/data/runtime identities; original-source full seed0 GPU replay; all twenty saved-checkpoint CPU replays; independent batch/pooled AP and candidate RNG reconstruction. Each layer has a separate JSON artifact.

## Delivery gates

The final delivery checker requires complete experiment evidence, aligned canonical notebook definitions, three student blanks, executed solution cells, four portable images, deterministic rebuild, desktop/mobile controls, keyboard/reset, prediction/teachback gates, print/no-JS, zero JS failures, and links/assets in a copied tree built using the actual Pages workflow. Visual screenshots are inspected separately. Cloud billing is represented as measured resource estimate, not an invoice. Live Colab and deployment remain NOT_CHECKED.

Exact final statuses and results live in `labs/evidence/l110/summary.json`, `labs/_replay_l110_results.json`, `labs/_delivery_l110_results.json` and `labs/l110-reproduction.md`. Learner status remains PENDING_WRITTEN_DEFENSE.
