# Lesson 078 delivery design

Authorized scope: curriculum bridge, manual one-hop mean aggregation, MPNN abstraction, GCN normalization, and full reproducibility. Use a short core route plus an optional complete Cora Table 2 experiment. An arithmetic-only lab would omit the requested published-experiment lane; a QM9 reproduction would displace the planned bridge. The chosen Cora lane keeps the entire two-layer model, preprocessing, fixed split and trainer visible.

Core deliverable: synchronous source-to-destination routing, mean reduction, self-update, empty-neighbor behavior, permutation equivariance, two-hop reach and point-in-time boundary. Reusable message-passing control exposes messages, reduction and update, with baseline retained. GCN visual separates symmetric normalization from row mean. Architecture overview carries shapes into portable notebook figure.

Verification: hand and dense numerical oracles, split assertions and pinned bytes, 100 independent initialization seeds with published configuration, executed solution, student TODO checks, browser desktop/mobile controls and print, copied Pages staging links. Report modern-framework and original-seed differences, original TensorFlow parity unrun, and no QM9 reproduction claim. No learner mastery inferred.
