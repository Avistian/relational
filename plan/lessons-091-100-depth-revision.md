# Lessons 091–100: typed computation to defensible prediction

User-confirmed scope: 91–100, with lessons 40–70 as the quality reference; publish to main.
Baseline: 657c4433aa608cacbeb1f002e5a3d670568c9d3b.

Retain the existing rigorous local derivations and visible lab implementations. Add a guided whole-system walkthrough after retrieval, rather than replacing the lessons or creating a separate navigation system. Each walkthrough connects a prior limitation to a concrete mechanism, traces an extra example, names the primary-source locator, and hands a testable question to the next lesson.

Reuse the course typography, accessible horizontal diagram containers, editable SVG renderer and portable notebook exports. Diagram topology follows the computation: relation branches for R-GCN, route branches for HAN, score/value branches for HGT, and explicit query/loss boundaries for sampling. Survey and construction lessons show workflows, not invented models. Add a compact linked reference map.

Canonical markdown, lesson builders, student/solution notebooks and HTML previews must agree. Preserve code, execution counts, saved outputs and scientific evidence status. This is an editorial revision, not a new training run or evidence of learner mastery.

Validation: rebuild all ten packages; check source links and local anchors; compare notebook code/outputs with baseline; check deterministic representative rebuilds; inspect desktop/mobile complete pages and SVG bounds; copy actual Pages staging; commit/push main, await successful deployment and compare published bytes.
