# Model fidelity and benchmark claims need separate evidence

L049 source review established that Trompt v2 claims comparability with trees, with subgroup exceptions, while ExcelFormer v5 makes stronger claims over its evaluated suites. This corrects the curriculum's shorthand; it is an authoring finding, not evidence of learner mastery. The selected numeric ExcelFormer forward path and input gradients match pinned released code exactly on copied weights; positional tie masking and initialization details must be distinguished from broad paper wording.

The local three-task, three-seed author-split experiment detects no overall rank difference (Friedman p=0.607; CD=1.914); banknote saturates. A separate real-timestamp MovieLens pipeline reverses its local neural/tree ranking across random and chronological regimes, which changes the test population and cannot refute the paper's IID claim. The full-width Pima attempt gives Feat-Mix AUROC 0.8261 ± 0.0057 (sample SD), versus the cited 0.8356 target, but remains INCOMPARABLE because search, sampler, seed aggregation and upstream preprocessing alignment are incomplete. Trompt Eq.4/5 are implemented as key parts, not a full trained encoder. Carry these boundaries into L050's fair-comparison checkpoint.
