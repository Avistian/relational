# GitHub Pages enabled

Static site mirrors the chess teaching workspace: `index.html`, `assets/`, `lessons/`, `reference/` deployed via `.github/workflows/pages.yml`. Target URL: https://avistian.github.io/relational/ — requires push to `main` and Pages source set to GitHub Actions in repo settings.
