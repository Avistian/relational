# Lesson 003 prepared — train / valid / test & CV

Published `lessons/0003-train-valid-test-cv.html`, reference `cross-validation-splits.html`, and reusable `assets/split-viz.js` (stratified vs random fold balance). Primary reading: sklearn cross_validation module. Lab: stratified `train_test_split` + `cross_val_score` on train only.

**Implications:** User has prior grouped-CV experience — lesson explicitly defers grouped and temporal splits to 004 and 021. Exit when lab printed class rates and CV mean ± std.
