# Lesson 011 started — decision trees as partitions

User began Lesson 011 (Q2 start; gradient-boosting arc). Prior signal: Q1 (001–010) marked complete in NOTES after L010 checkpoint; no separate L010 start/complete learning record on file — retrieval will probe the leakage spine and baseline numbers cold.

**Retrieval check:** warm-up **4/4** (B: PR baseline = prevalence; B: HistGBDT wins on threshold-like interactions; C: fit-bearing scaler inside Pipeline; B: wire leakage spine). Q1 checkpoint retained cold.

**Focus this session:** read a depth-1 tree as an axis-aligned partition; connect split geometry to Gini impurity reduction; lab on OpenML German credit via `relkit`.

**User preference (2026-07-02):** labs need richer in-notebook intros (concept recap, per-task goal/why). Standard recorded in NOTES #8 + `lab-authoring` skill; Lab 011 retrofitted.

**Exit:** notebook `labs/0011-decision-trees-partitions.ipynb` prints split feature/threshold + hand-computed ΔGini; lesson quizzes pass; or "lab done." Next: Lesson 012 (bagging & random forest).
