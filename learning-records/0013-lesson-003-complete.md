# Lesson 003 complete — train / valid / test & CV

User completed Lesson 003 lab and warm-up retrieval. Warm-up: country feature safe at `t` (static, not back-filled); design matrix `(X, y)` as structural forcing mechanism for joins — both correct cold. Exit via "lab done" (stratified split + CV on train only per lesson ticket).

**Evidence:** warm-up answers in chat; lab completion self-reported.

**Implications:** Lesson 004 (grouped & nested CV) unlocked. User already uses grouped CV in homework — L004 should connect sklearn `GroupKFold` / nested CV to their existing pipelines rather than re-teach i.i.d. splits. Optional: audit `report.md` CV section before L004.
