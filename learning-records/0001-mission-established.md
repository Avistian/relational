# Mission and baseline established

The user’s goal is not generic ML literacy but proving a specific thesis: relational deep learning and foundation relational models are undervalued relative to single-table + feature-engineering workflows. They commit ~1 hour/day over a multi-year arc. Prior hands-on tabular work exists (pipeline design, grouped cross-validation, missingness strategy) — future lessons should assume sklearn fluency and focus next on *why* single-table ML is the default and where it breaks on multi-table data.
