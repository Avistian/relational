# Checkpoint evidence has a protocol and an experimental unit

L050 authoring verifies a complete numeric ReGLU FT-Transformer evaluation path against rtdl on copied weights, while its fixed-budget three-task comparison remains distinct from the paper benchmark. Seed repeats share the same split and do not create extra datasets: the primary FT-T/XGBoost comparison has two wins and one loss, exact sign p=1.0; the three-arm Friedman test has p=.368. These are author measurements, not evidence of the learner's mastery.

The required larger Higgs Small attempt also ran (6000 rows, three seeds) and remains INCOMPARABLE to the paper's Table 2 target. Future interpretation must distinguish individual-model metrics from Table 4's ensemble comparison, and distinguish conditional seed intervals from uncertainty over new tasks or time periods. The next quarter should carry this baseline/evidence discipline into its modern-tabular-model comparisons.
