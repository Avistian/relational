# Approved Lesson 116: Debug GNN training

Approved by the user in chat. Teach symptom -> competing hypotheses -> minimal failing probe -> one-variable intervention -> regression evidence. Cover flat loss, exploding/vanishing gradients, over-smoothing, bad seed/local indexing and held-out-label leakage. No new model; extend L085 and L112-L115 toward the L117 RDL bridge.

Deliver a detailed HTML lesson, three live TODO/CHECK tasks, standalone student and executed solution notebooks, portable computation/diagnostic figures, interactive probes, quick reference, full visible model/trainer, pinned sources, exact commands and protocol/deviation ledger. Learner remains PENDING_WRITTEN_DEFENSE. No publication requested.

Named experiment: OGB v6 Table 6 ogbn-arxiv GCN, complete raw release and official time split, ten fresh seeds 0-9, 500 epochs each, three GCN layers with two hidden BN/ReLU/dropout blocks, train-only mean NLL, Adam .01, first-best validation checkpoint including BN buffers. Targets validation 73.00 and test 71.74 percent; predeclared descriptive CLOSE tolerance 0.5 pp. Pin original commit 61e9784ca76edeaa6e259ba0f836099608ff0586. Current-runtime source parity and fresh checkpoint replay do not establish historical or whole-paper identity.

Course interventions: paired missing-step/repaired 80-epoch full-data traces, separate from benchmark runs. Local deterministic fixtures isolate seed indexing, label access, gradient amplification and repeated smoothing; these are mechanism demonstrations, not additional paper benchmarks. Keep an unchanged initialization, graph and RNG when comparing each fault/repair.

Budget: USD10 aggregate including all seeds, pilots, retries and validation. Current Modal T4 + 2 physical CPU cores + 8 GiB costs USD0.00020796/s. At most twenty 600-second worker reservations gives USD2.49552 worker ceiling, leaving USD7.50448 for overhead/storage. No automatic retries. Pilot first, check correctness and projected completion before ten full fits. Stop if aggregate projection exceeds budget, never shorten a run and call it complete.

Implementation sequence:
1. Establish failing behavioral probes; implement three task functions and a source-preserving instrumented trainer.
2. Verify original outputs/gradients/update, dataset identity and diagnostic red/green cases; pin source hashes.
3. Run bounded pilot then ten fresh fits and two controlled fault/repair traces; collect and independently rescore/replay checkpoints.
4. Author narrative, interactive diagnostics, figures, notebook tasks and reference from measured evidence.
5. Execute standalone solution; verify meaningful mutations, reproducible builds, desktop/mobile/keyboard/no-JS/print and copied Pages staging.
6. Register manifest and evidence, preserve existing work and learner status.

The brainstorming transition helper writing-plans is not installed. This approved document supplies the implementation plan directly.
