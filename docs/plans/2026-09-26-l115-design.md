# Approved Lesson 115: Graph ML design patterns

Approved in chat: encoder → message passing → task head, with three task-design exercises, visible standalone implementation, executed solution, reference sheet and architecture diagrams. Connect L076 and L114 to L131. Preserve heterogeneous type semantics, graph boundaries, temporal visibility and task-specific evaluation.

Named experiment: OGB v6 Table 6 GCN on complete ogbn-arxiv, ten fresh seeds 0–9, 500 epochs each. Identity encoder; two hidden GCN/BN/ReLU/dropout blocks; final GCN and log-softmax head. A linear replacement would change the model. Pin original OGB commit 61e9784ca76edeaa6e259ba0f836099608ff0586. Audit output/gradient/update parity before paid training; independently rescore and replay every selected checkpoint afterward. Numerical closeness does not establish historical or whole-paper identity.

Budget: USD10 aggregate, including pilot, seeds, retries and validation. Up to twelve 600-second workers at USD0.00020796/s = USD1.497312 worker ceiling, USD8.502688 reserved for overhead. No automatic retries. Pilot must pass correctness and project sufficient runtime before the full run.

Implementation sequence:
1. Write failing modular parity and node/link/graph behavioral checks; implement visible canonical module.
2. Pin provenance, independent source checks, budget and bounded worker. Run pilot; validate; run ten fresh fits.
3. Collect hashes, independent metrics and original-model checkpoint replays.
4. Write detailed lesson, three live notebook tasks, architecture/computation figures, interactive task explorer and reference.
5. Execute standalone solution; reject meaningful mutations; check deterministic builds, desktop/mobile/keyboard/no-JS/print and actual copied Pages workflow.
6. Add manifest-driven navigation and record exact execution boundaries. Learner remains PENDING_WRITTEN_DEFENSE. Publishing not requested.

The brainstorming skill's optional transition dependency writing-plans is absent from installed skill directories. This explicit implementation sequence supplies the necessary plan without that helper.
