# Lesson 109 — database timestamp contracts

User approved the combined scope: a detailed lesson and complete selected RelBench rel-f1/driver-position reproduction. Teach event/availability clocks, corrected historical versions, as-of joins, node/edge timing and fit-label maturity. Implement visible historical selection and SQL-oracle graph checks; ship standalone student/solution notebooks, portable figures, interactive timeline, reference, provenance, protocol/deviation ledger and exact commands.

Reproduction: pin RelBench v1.1.0 source/data; regenerate all released train/validation/test labels from database tables; compare full query identities and labels; rerun all five Table 4 heuristic baselines on validation/test. Preserve release quirks explicitly. Do not call a selected deterministic slice the entire paper. Real ingestion histories are absent; delayed/corrected records are synthetic course evidence. No learner mastery is inferred.

Budget: local CPU only, USD0 cloud spend expected; USD10 total ceiling includes retries/validation. Abort any individual local experiment at 10 minutes and diagnose before expanding. No paid compute necessary. Do not train a GNN or change the approved reproduction target.

Sequence: source and data audit -> failing contract tests -> visible version/join/graph implementation -> full task regeneration and source prediction comparison -> independent SQL/mutation checks -> prose/figures/widget/notebooks -> clean-directory solution execution -> browser/mobile/no-JS/print/copied-Pages checks -> evidence reconciliation.

writing-plans skill is absent from the provided catalog and searched skill directories; this document records the implementation sequence. Existing unrelated workspace modifications are preserved. Approval is already granted; no further design gate is needed.
