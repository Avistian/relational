# Lesson 103: approved TGAT Wikipedia package

User approved the combined scope on 2026-09-26: self-contained lesson, student/solution notebooks, full visible TGAT model/trainer, complete ten-run Wikipedia released-experiment replay, and a separate common-protocol TGAT/TGN comparison. No publication requested.

## Design and implementation sequence

1. Pin Xu et al. 2020 and the publication-era release; audit preprocessing, sampler, architecture, objective, selection and metrics.
2. Implement a focused TGAT port. Test source output/gradient parity, temporal cutoffs, all-padding behavior, recursive times and live student functions before training.
3. Execute a CPU pilot and use the same implementation on GPU for the selected full-data ten-run replay. Retain predictions, traces, configurations, environment and hashes. Report blockers and numerical failures candidly.
4. Run a separate three-seed bounded Wikipedia comparison with a shared split, event budget, negative schedule, selection and metric. Keep architecture differences explicit; do not claim benchmark superiority.
5. Build HTML, reference, portable diagrams, student/solution notebooks, inline reproducible commands and gated scale-up; update manifest and course records.
6. Execute the solution, independently reconstruct metrics, check source parity and desktop/mobile rendering in a copied Pages tree.

## Frozen evidence boundaries

Named original targets: Xu et al., Tables 1/2, Wikipedia TGAT AP 95.34 (0.1) and 93.99 (0.3), ten runs. Course numerical tolerance: absolute mean gap <= 0.5 percentage point, declared before runs. A close number is insufficient for protocol equality. The release evaluates all future events and a new-node subset, unlike the literal paper description. Historical parity INCOMPARABLE; full-paper parity NOT_ESTABLISHED. Reddit, industrial and node classification remain outside this approved slice.

Important source quirks to preserve in the release lane: truncated neighbor prefix, last-event omission, finite padding mask, shuffled-but-unused index list, relative early-stop tolerance .001 and shifted checkpoint index. Correctness-oriented shared-protocol experiments will be labeled separately. Public source has no license file; fetch it locally for checks rather than incorporating its complete files into the distributed lesson.

## Execution complete

All ten declared runs, original-model seed-0 replay, independent prediction metrics, standalone notebook and copied-Pages/browser checks completed. Exact results and identities: `labs/_paper_l103_results.json` and `labs/reproductions/execution_evidence.json`. Deployment and live Colab remain NOT_CHECKED.
