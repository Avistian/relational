# L110: Q3 temporal GNN checkpoint

Combined scope approved by the user on 2026-09-26. Deliver a complete self-contained teaching package and fresh ten-seed TGN-attn Wikipedia released-protocol reproduction, plus ten paired clean-state runs. The clean arm restores selected weights, memory, clocks and queued messages together and keeps equal-time events within one batch so that memory never crosses a strict-time boundary. These changes are a course intervention, not the published protocol. Both arms use complete released populations, explicit seeds 0–9, max 50 epochs, patience 5, validation-only selection. The release arm retains final-epoch selection on reaching the maximum; clean arm always selects best validation state. Predeclare paper CLOSE tolerance 0.5 pp for each ten-seed mean; retain historical identity INCOMPARABLE and full-paper NOT_ESTABLISHED.

Teaching sequence: retrieve L102/104/108/109 -> trace score-before-update -> distinguish weights from temporal state -> construct split and candidate contract -> run leakage counterexamples -> interpret published and paired course results -> defend the checkpoint. Three live notebook tasks: atomic checkpoint restore, tie-safe batching, and event/availability eligibility. Full model/trainer inline, portable computation figures, reference, author-reference evidence versus fresh learner execution, and PENDING_WRITTEN_DEFENSE.

Implementation: behavioral tests first; reuse audited L102 encoder without changing it; expose checkpoint and batching changes in a canonical L110 module; source parity for release arm; independent raw/split and metric audits; bounded GPU pilot then full runs; save predictions/checkpoints/traces; execute solution; browser desktop/mobile/keyboard/no-JS/print and realistic copied-Pages checks. The writing-plans skill is absent from the installed catalog; this document includes its implementation-plan substitute. No publication requested.

Budget: USD10 total including all seeds, pilot, checks and retries. T4 .000164/s + two CPU cores .0000262/s + 8 GiB RAM .00001776/s = .00020796/s, about .749/hour (Modal pricing checked in chat). Historical 293 epochs per ten seeds at about 43 sec/epoch suggests about seven aggregate hours for twenty fits (~USD5.25), plus overhead and checks. Pilot validates estimate. Cap total GPU function runtime at 10 aggregate hours (~USD7.49), reserve USD2.50 for overhead/storage; no automatic retries. Per-run allocation and remaining budget recorded before launch; incomplete work is labeled rather than overrun.
