# Lesson 095: the user–item graph is an evaluation contract

The user requested the next planned lesson and full reproduction. Follow the existing L095 curriculum: typed HeteroData, bipartite link splitting, projection pitfalls and a recommendation baseline. The mission is leakage-safe relational prediction; mastery remains pending the learner's written defense.

Options considered: a toy-only graph misses the reproduction request; introducing LightGCN would replace the assigned scope with a new model paper; the chosen package combines a worked graph with a full ML-100K release audit and a completely executed, explicitly course-defined ranking experiment. No assigned model paper exists, so historical model-score reproduction is not asserted. The named published target is the GroupLens README's full counts and five released partitions. The course extension measures all five folds with popularity, a three-hop walk, and a validation-selected mixture. No sampling, reduced catalog or shortened run stands in for the full target.

Data flow: download/hash the archive, verify full row identities and partitions, reserve inner validation within each official base, build likes and reverse likes from fitting rows, estimate transition probabilities, select mixture using validation NDCG@10, refit on the complete official base, rank all unseen catalog items and report user-macro Recall/NDCG@10. Explicitly retain low ratings in the seen-item mask, separate types even when IDs coincide, and reject duplicate pairs. Isolated users get a stated popularity fallback. This is random offline completion, not temporal forecasting. Three live notebook tasks build the typed graph, enforce the boundary and calculate the walk. The evaluator, loader, full five-fold loop and plotting are provided visibly.

Deliverables: canonical prose/HTML, complete source and runner, student/solution notebooks, reference sheet, protocol and source ledger, arithmetic/behavioral/independent metric checks, full raw top-10 rankings, pipeline/walk/projection/result figures, reusable interactive controls, manifest/gallery integration, Pages packaging and actual desktop/mobile/print inspection. Validate fresh standalone notebook results against the runner and preserve outputs only when code is identical. Keep distribution data out of git. No deployment, live Colab or learner completion claim.
