# Lesson 106: temporal link prediction

User approved the proposed design in chat on 2026-09-26.

Teach one skill: defend a future-edge evaluation by tracing its history, candidate population, scores and aggregation. Retrieval -> worked EdgeBank example -> candidate intervention -> full Wikipedia replay -> written defense. Explain repeated interactions versus first-ever edges and distinguish candidate discrimination from a forecast of all future events.

Reproduction: Poursafaei et al. NeurIPS 2022, Wikipedia rows, Tables 2/3/4/6/8/10 (arXiv v2 numbering). Two EdgeBank memories (unlimited and released fixed window), three samplers, five released run iterations, complete split. Predeclare CLOSE as both AP and AUC within 0.015 absolute of two-decimal published targets; this tolerance is a diagnostic, not historical equivalence. Preserve source/data hashes, held-out nodes, seed behavior, batch aggregation and deviations. Freeze before observing results. Entire multi-dataset/neural-model benchmark NOT_RUN.

Use publication-era DGB commit 7793e9449f5321c7e39b24c0585e3c3de7cf9f5e. Inspect source versus prose explicitly: quantile window versus fixed duration, random source replacement after exclusion, global versus instance RNG, Python set iteration and historical runtime differences. No silent fixes to claim parity. Independent visible implementation must match original memory predictions on every replayed batch. Strict timestamp/availability fixtures belong to teaching lane, not silently substituted source evaluation.

Artifacts: Markdown+HTML, standalone student/solution notebooks with live TODO/CHECK/EXIT, prepared HTML, portable computation figures, candidate widget, source/protocol ledger, raw-data runner, per-batch predictions, checks and reference. Manifest-driven navigation only. Preserve unrelated changes.

Validation: adversarial causal/candidate/metric fixtures, full-source comparison, full executed notebook, mobile/desktop/no-JS/print checks and copied Pages staging. No learner mastery inferred. No deployment or live Colab claim.

Compute: local CPU, no paid launch, $0 cloud. Up to 45 minutes replay cutoff; record incomplete work if reached. Cache only authenticated complete generated candidates; fingerprint input/source/config and fail closed. No fabricated independent seed variability from identical reset streams.

Implementation sequence: pin source -> write semantic checks -> implement visible model/evaluator -> generate original candidates and run all conditions -> author narrative/figures/notebooks -> execute solution -> integrate and verify delivery. The writing-plans skill is unavailable in the installed catalogs; this document carries the implementation plan.
