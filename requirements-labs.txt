# Lab notebooks — Year 1 tabular foundations (Lesson 006+)
# Install: python3 -m venv .venv && .venv/bin/pip install -r requirements-labs.txt

numpy>=1.26
pandas>=2.1
matplotlib>=3.8         # Notebook experiment reports and portable paper figures
scikit-learn>=1.5
imbalanced-learn>=0.12   # Lesson 007 — SMOTE + imblearn Pipeline
xgboost>=2.0            # Lesson 014 — XGBoost (Chen & Guestrin 2016)
lightgbm>=4.0           # Lesson 015 — LightGBM (Ke et al. 2017)
catboost>=1.2           # Lesson 016 — CatBoost (Prokhorenkova et al. 2018)
torch>=2.2              # Lesson 028 — MLP & ResNet baselines (first PyTorch lab); CPU build is fine
rtdl_num_embeddings==0.0.12  # L070 pinned original TabM comparator
rtdl_revisiting_models>=0.0.2  # Lesson 041/042 — reference MLP/ResNet/FT-Transformer (Gorishniy 2021)
pytorch-tabnet>=4.1      # Lesson 043 — reference TabNet; VALIDATION only (our TabNet is from scratch)
entmax>=1.3             # Lesson 044 — reference entmax15/sparsemax; VALIDATION only (our NODE is from scratch)
pyarrow>=15.0            # labs/data cache (parquet)
ipykernel>=6.29
jupyterlab>=4.2

# L057 optional pretrained inference baseline: see labs/requirements-l057-tfm.txt.
# Main OOF notebook and archived three-family audit need no additional package.

# L074 default notebook uses the dependencies above and a compact FastText cache.
# Optional source-parity/cache-rebuild tools: labs/requirements-l074-audit.txt.

# L074 paper-era CARTE replay (model and trainer remain visible in the notebook).
torch-geometric>=2.5.3
fasttext-wheel==0.9.2
torcheval==0.0.7
skrub==0.10.1

# L075: typed materialization and row-encoder API audited against release 0.3.0.
pytorch-frame==0.3.0

# L078: execute original GCN preprocessing as an independent oracle.
networkx>=3.2

rdkit==2025.9.6  # L081 — explicit QM9 chemical features

# L089: graph partitioning (exact lesson pins in labs/requirements-l089-runtime.txt).
pymetis==2025.2.2

# L093: released OAG graph serialization.
dill==0.3.8

# L098 native NeighborLoader requires a torch-compatible pyg-lib build.
# Pinned native source and CPU install: labs/l098-reproduction.md (also used by L086).

# L109 original RelBench SQL audit; pandas 3 string dtype support.
duckdb>=1.5.5
