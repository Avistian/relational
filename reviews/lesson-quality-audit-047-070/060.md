# Lesson 060 — broad model comparison

## Verdict

**Before: substantive repair required.** The package stated an appropriate broad comparison goal but relied on a generic three-task notebook, underdeveloped metric/statistical instruction and an incorrect historical TabM-mini dependency. The historical 210-record result was real, but could not validate the intended corrected mini variant. TabReD evidence had index hashes without explicit sampled IDs in the result; complete-panel validation inferred its roster from observed results, so a wholly missing task could disappear. Asymptotic Friedman p-values for three temporal tasks were insufficiently distinguished from calibrated tests. The generic reproduction text discussed irrelevant checkpoint/PFN paths.

**After: repaired local teaching package, original paper results not reproduced.** The scope is now a five-operation protocol checkpoint with visible reused model/trainer source, 210 newly fitted corrected records, explicit dataset rosters and row IDs, independent artifact reconstruction, conditional permutation calibration, substantial worked explanations, a connected portable protocol figure, a live aggregation counterexample, readable seed plots and a concrete EXIT. The local paper status remains INCOMPARABLE. The convenience panel, reduced recipes, already inspected splits and machine-load timing prevent stronger conclusions.

The author did not change MISSION.md, learner mastery, shared registries, global delivery records or publication workflow. Parent owns shared routing, gallery/access metadata, commits and publication. Teacher notebook and student EXIT output remain local/ignored under the course convention.

## Baseline and teaching scope

Read the assignment BRIEF, teach/pedagogy/lab/visual skills, MISSION and relevant NOTES, then the old manuscript/depth/reference, generic builder/notebook configuration, benchmark_core, foundation_benchmark, temporal release loader, model dependencies, saved result metadata and representative 042–045 lesson/lab patterns. The baseline requirements applied were visible mechanics, why/goal/hint guidance, direct computation checks, numerical worked examples, multi-dataset seed evidence, explicit scale-up and source/result boundaries. The revised manuscript has over 3,000 words before the retained depth explanations; length supports tracing the computation rather than serving as the acceptance criterion.

This lesson introduces no new model. Its principal diagram is an end-to-end evaluation protocol. Neural models from earlier lessons are inlined in coherent chunks so the notebook remains inspectable: first-input/member operations, shared backbone and heads, robust scaling/NTP primitives, RealMLP architecture/groups, train-only encoders, fitter, then full runner. Tree libraries remain comparator boilerplate. Five live code tasks reach both analysis and fresh fitting. The smoke is explicitly one-task/one-seed execution evidence, not a comparative study.

## Claim / primary source / implementation / evidence inventory

1. **Evaluation is a procedure, not a model name.** Read TabArena v1 full main exposition, especially §§2.1–2.3 and 3.2, and appendices A.1–A.5, B.1–B.3, C.1–C.3 and D.1–D.4. Checked inner ensembles, candidate budgets, outer repetitions, saved predictions, metric choice, data curation, model-specific preprocessing and CPU/GPU protocol differences. Local `checkpoint_l060_v2.run_checkpoint` implements a declared reduced fixed-holdout procedure; it does not implement those nested ensemble protocols. Current official repository has evolved, so the manuscript names paper v1 rather than equating main with paper release.
   - https://arxiv.org/html/2506.16791v1
   - https://github.com/autogluon/tabarena
   - Pinned local official leaderboard source `labs/sources/l056/official_paper_leaderboard.py`, revision `e7cc6b049f9be11a6df29eb2560d9ccb2399d95c`, helps maintain continuity with the earlier frozen reanalysis. No new official leaderboard training is claimed.
2. **Temporal evaluation can change relative gaps.** Read TabReD v1 exposition/appendices and then checked v4, including §5.4/Figure 2 and appendices A–C. Corrected earlier-version section numbering in the new text. Official experiment uses three paired splits and 15 initializations; local uses three tasks, one pair, three seeds, reduced rows, altered roster/metric/feature treatment. The Sberbank released target was checked against the pinned preprocessing source, not inferred from its dataset name.
   - https://arxiv.org/html/2406.19380v4
   - https://github.com/yandex-research/tabred/tree/b5ef15b3749f30da7a1eb8fba21a5b54d706bf32/paper
   - https://raw.githubusercontent.com/yandex-research/tabred/b5ef15b3749f30da7a1eb8fba21a5b54d706bf32/preprocessing/sberbank_housing.py
   - Local `temporal_experiment.load_release`, `checkpoint_l060_v2.run_checkpoint`; actual release data hashes and sampled IDs in the new result. `strict_time_boundaries:false` is retained for Sberbank train/validation timestamp ties. Label arrival and aggregate-feature availability remain unchecked semantic questions.
3. **TabM-mini mechanism.** Read TabM v3 §§3–5, practical-limit material in Appendix A, D.2/D.3 preprocessing/training/ranking and D.8/D.9 variant details. Inspected pinned official source and the corrected teaching implementation. Mini keeps only the first input adapter, shares backbone biases and has distinct heads; objective averages member losses, prediction averages member probabilities. k=8/shared batches/standardized inputs/no dagger embeddings/small fixed epochs are local deviations.
   - https://arxiv.org/html/2410.24210v3
   - https://raw.githubusercontent.com/yandex-research/tabm/28e47ae301c92ec37787dde1ce923a0793f405b4/tabm.py
   - Local `labs/relkit/tabm_v2.py`; current hash matches `labs/_source_check_l054_results.json`'s copied-weight forward/backward checked implementation. The L060 checker freshly checks mini topology and fan-in bounds. This audit reused the unchanged parity evidence; it did not rerun official full training parity.
4. **RealMLP recipe rather than generic MLP.** Read §§2–5, A.1/A.2, A.5, B.3/B.5/B.11 and C.6/C.7 of the v2 paper; inspected official standalone `mlp.py` and local `realmlp.py`. TD-S one-hot inputs, omitted embeddings, non-parametric activations, zero output initialization, robust scale/smooth clip, NTP and group multipliers remain visible. Main best-epoch metric, 256 epochs and full TD/benchmark are not locally reproduced. The shortened run traverses the complete compressed schedule, rather than taking a prefix of the paper run.
   - https://arxiv.org/html/2407.04491v2
   - https://raw.githubusercontent.com/dholzmueller/realmlp-td-s_standalone/a8c73f75dbeae4ab1ba5bc92372c444ffa0f766e/mlp.py
   - `labs/_source_check_l053_results.json` remains existing numeric primitive/gradient/optimizer evidence; `checkpoint_l060_v2.fit_candidate` is the current reduced recipe. No claim of whole-procedure parity.
5. **Ranks and uncertainty.** Demšar §3.2 supplies the multi-dataset Friedman/Nemenyi framework and small-sample approximation caveat. Local `aggregate_panel` explicitly validates the expected product before averaging seeds and ranking tasks. `paired_effect` joins by seed and subtracts before estimating conditional spread. `rank_audit_l060.rank_permutation` calibrates fixed within-row rank dispersion with 20,000 random permutations or exact 14,400 temporal arrangements. It assumes method-label exchangeability under the null and does not justify transfer to a random enterprise population.
   - https://jmlr.org/papers/v7/demsar06a.html
   - `labs/_analysis_l060_v2_results.json` contains reconstructed summaries, per-task effects and both calibration modes.

The machine-readable full inventory is `labs/_sources_l060_v2.json`. It distinguishes original claims, current implementation, existing source checks, new measurements and unrun paper protocols.

## Concrete repairs

- Kept historical `foundation_benchmark.py`, `benchmark_core.py`, `tabm.py` and `_verify_l060_results.json` unchanged. Created scoped `checkpoint_l060_v2.py` and `comparison_l060.py`; `_verify_l060.py` now calls the corrected runner.
- Refit all 14 dataset/regime cells × five arms × three seeds; no copied historical scores are presented as corrected results. The result identifies actual library versions and hashes every relevant local module, input archive-derived data files and sampled row order.
- Added explicit validation of integer one-dimensional partitions; stable finite validation selection; from-scratch scalar binary log loss and RMSE with input validation; expected-roster crossed-panel aggregation; seed-keyed paired effects.
- Added per-record test IDs, class order and per-candidate selected epochs. Search timings explicitly include losing candidates and validation. Existing output paths are rejected; partial failure artifacts remain IN_PROGRESS and cannot generate a complete summary.
- Expanded explanation of frozen family selection, train-only categorical vocabularies, all-missing numeric zero fill, metric contributions, nonlinear rank/average order, t intervals, task blocks, permutation null, Nemenyi normalization, regime composition confounding and conditional cost decisions.
- Replaced generic `_build_l060.py` with a canonical complete scoped builder. It emits student/solution/prepared HTML, lesson, reference and portable diagrams while preserving depth integration. Parent integrated generic delegation and the five task signatures separately.
- Added a seven-decision ledger before the first result audit, optional local retrieval of L059's ledger, a source-bearing EXIT and a fresh five-arm smoke with actual live call counters. Teacher interpretation cites the Ecom reversal and Homesite counterexample rather than generic text.
- Added connected protocol and aggregation figures, corrected measured seed plots and rank/CD plot. The live slider changes A's second seed loss and shows both rank-of-mean and mean-of-seed-ranks. Native controls, reset, keyboard-focusable horizontal table, nowrap numeric tokens and full-size image links support narrow screens.
- Replaced unrelated generic reproduction language with exact corrected smoke/lab/closer commands, no-resume behavior, a gated live notebook extension and the shared CPU Modal entrypoint. Full original TabArena/TabReD alignment steps are separate.

## New evidence and conclusions

`labs/_verify_l060_v2_results.json`: **210 selected records, COMPLETE**; total recorded selected-predict-plus-all-candidate search cost approximately 664.99 s. The host had severe variable background load early in this run, so those measurements are timing records, not a controlled speed comparison. After stale editor status workers were cleared by the parent, remaining fits were much faster. Do not interpret this as an algorithmic change.

Random panel: 11 task blocks; mean ranks CatBoost 1.81818, XGBoost 2.90909, RealMLP-TD-S 2.90909, TabM-mini 3.18182, MLP 4.18182. Asymptotic Friedman p≈0.0139; 20,000-draw conditional permutation p≈0.00915. Temporal panel: three task blocks; TabM-mini 2.0, CatBoost 2.66667, MLP 3.0, XGBoost/RealMLP-TD-S 3.66667. Asymptotic p≈0.6626 versus exact permutation p=0.7199305556. No broad temporal winner is established.

Ecom Offers: TabM-mini minus XGBoost random log-loss gap +0.0417968 (conditional t95 [0.0310890,0.0525047]); temporal gap −0.1131406 (t95 [−0.1786826,−0.0475986]). The gap changes by −0.1549374, a useful local reversal. Temporal Homesite's same gap is +0.1160204, so the Ecom result is not a universal neural advantage. These are fixed-row three-seed contrasts; they do not identify a sole causal explanation.

## Checks actually run

- Test-first five-operation fixtures initially failed because the scoped module was absent; implemented functions then passed. Checks cover malformed partitions, overlap, stable ties, nonfinite errors, metric shape/range, complete roster including a completely absent dataset, mean-first counterexample and row-order-independent seed pairing.
- `_check_l060_v2.py`: PASS; all 210 reconstructed metrics agree with independent scikit-learn evaluators, maximum absolute difference 1.11e−16. Four artifact mutations rejected (target, row order, candidate choice, wholly absent dataset). All measured dependency hashes agree; corrected mini topology/fan-in and unchanged prior parity implementation identity pass. Saved `_check_l060_v2_results.json`.
- `_figures_l060_v2.py`: PASS; fresh evidence audited before figure generation; exact temporal enumeration 14,400 and random Monte Carlo 20,000. Saved `_analysis_l060_v2_results.json`. Inspected exported protocol, rank and temporal score figures at actual image size and repaired the protocol arrow/label geometry.
- `_build_l060.py`: PASS; canonical lesson/reference/student/solution/preview generated. Five TODO student bodies remain blank; provided code does not overwrite them.
- `_execute_l060.py`: PASS; **32 code cells** executed, including all audit, mutation, permutation, live call-count and fresh five-arm checks and concrete EXIT. Saved `_execution_l060_v2_results.json`. Larger local gate remains off. The solution is local/ignored. Fresh notebook and gated larger-run artifacts explicitly mark repository file hashes as reference-only and attach live function/class-method source plus PRESETS with a stable digest. The standalone canonical 210-record operator is unchanged.
- Parent independently reconstructed all 210 metrics/choices/raw targets/IDs/hashes and exact temporal calibration; see `060-independent.json`. This is independent arithmetic/provenance validation, not another fit.
- Local Chromium at 900 and 375 pixels: PASS, no console errors, missing assets, page overflow or broken numeric/header tokens. Exercised aggregation reset and slider endpoints; inspected mobile widget screenshot and exported figures. `_browser_quality_audit.py` saved `060-browser.json`.
- Imported `check_lesson(60, require_review=True)`: PASS, 79 local links; depth prose, student/solution explanatory equivalence, blank student TODOs, all 32 executed teacher cells, image payloads and prepared HTML checked. Saved `060-content.json`; no aggregate delivery CLI invoked. A subsequent complete canonical regeneration preserved the exact 32-code-cell source/output/count digest. `_widget_check_l060.py` additionally passed the exact .8 tie, reset, native keyboard slider and horizontal keyboard scrolling. Live Colab, Modal and original paper training were not run by the author. Parent owns copied Pages and publication verification.

## Parent final integration

The parent independently checked all 210 prediction metrics against scalar arithmetic and the original cached/released labels, row IDs, hashes and candidate/epoch selections. All 56 paired intervals matched an independent t(2) quantile derivation. Independent rank counts reproduced both panels; all 20,000 random permutations and 14,400 exact temporal arrangements matched the calibrated results. A fresh five-arm smoke through the generic runner verified its corrected routing; its retained artifact is `060-generic-smoke.json`. These checks establish artifact arithmetic and identity, not repeated full training or original paper reproduction.

Final Chromium checks passed at 900/375 pixels; all 42 slider states, both tie boundaries, keyboard increment/reset and the scroll region passed. The parent inspected the protocol, temporal scores, adjusted rank figure and mobile screenshots. Copied Pages passed 141 links/fragments, and actual no-JavaScript and failed-manifest learner routes passed. Generic regeneration preserved exact package bytes. Shared builder/notebook/run routes, five-task metadata, only the lesson-060 directory card and evidence exports now point to the scoped corrected package. The retained depth's Pareto definition was aligned with the correct main-text definition; all 32 executable notebook sources remained identical and their actual outputs were preserved. Reports: `060-independent.json`, `060-independent-smoke.json`, `060-widget.json`, `060-browser.json`, `060-pages.json`, `060-access.json`, `060-regeneration.json` and `060-content.json`.

The user requested a pause after lesson 060. The newly started 061 author stopped without changing any files or starting runs. Lessons 061–070 remain pending until the user resumes.

## Residual limits

Corrected numeric architecture is not original benchmark reproduction. The course convenience roster, reduced epochs/width/member count, common categorical representation, omitted TabReD categories, current-release target differences, one temporal pair, three seeds, altered metrics, no full native categorical CatBoost and no original nested tuning/ensembling remain material. Three temporal task blocks are too few to support strong generalization, and all intervals are conditional. No new test population validates a post-hoc deployment winner. The value delivered is an inspectable, runnable comparison with substantive mechanics and honest inference boundaries.

Final author kernel-provenance check: fresh JSON is valid and explicitly marks repository file hashes as reference-only; its stable digest verifies 28 actual live functions/classes plus PRESETS. Live calls: partitions 17, selector 220, score 240, panel aggregator 3, paired effects 56. Source-identical full regeneration preserves all 32 code-cell outputs/counts exactly. Parent may now take stable ownership for final browser, staging, access and publication checks.
