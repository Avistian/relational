# Lesson 058 — surveys, meta-benchmarks and held-out-method selection

## Verdict before and after

**Before:** correct high-level warnings about task denominators and outcome selection, but the central paper contribution was under-taught. The lab parsed means and bootstrapped a six-method matrix; its deliberately XGBoost-favored45 subset could be mistaken for the actual TALENT tiny-benchmark construction. The survey distinction omitted that Borisov also runs an original five-dataset experiment. The parser silently converted malformed nonmissing cells to NaN, discarded SDs, and did not itself reject duplicate IDs. The generic post-EXIT checkpoint/download/scale-up language was irrelevant to a CPU frozen-table reanalysis. A method-column information boundary was recommended but not implemented.

**After:** substantial paper-grounded manuscript and standalone five-TODO lab. It reconstructs a specified interpretation of TALENT §8.2 random-subset mean-rank matching, evaluates held-out methods, exposes real missing-result exclusions, and runs the10,000-candidate track through the student's live definitions. Rich numerical examples, a predictive interactive selector, portable matrix/protocol and paired measured figures, explicit source disagreements, actual result interpretation, a useful reference and a precise CPU reproduction contract replace generic packaging. Original training/Table7 result reproduction remains **INCOMPARABLE / NOT_ESTABLISHED**; this is an actual implementation and measured reanalysis improvement, not only a disclaimer pass.

## Reading and claim inventory

Primary sources were verified through browser retrieval and full local HTML inspection of relevant sections. The inspected TALENT v3 HTML incorporates the detailed methods/analysis rather than a separately labeled appendix. The official pinned repository tree and evaluator/training entry points were inspected. Raw training arrays were not downloaded.

| Claim or mechanism | Primary source | Checked local implementation/evidence | Repair and limit |
|---|---|---|---|
| Surveys organize mechanisms but can also run original experiments | [Borisov v3 §§III–IV,VII–VIII](https://arxiv.org/html/2110.01889v3) | Manuscript taxonomy/protocol comparison; reference | Explicit transformations/architectures/regularization; five-task experiment is not merely literature synthesis |
| Borisov benchmark protocol and scope | Same §VII A/B | HELOC,Adult,HIGGS,Covertype,California;100Optuna iterations/fivefold described in prose | Paper-reported scope, not re-run; no universal winner or current family-superiority claim |
| Further reading map | [2024 survey §§2–3](https://arxiv.org/html/2410.12034v1) | Supplementary taxonomy paragraph | No unverified performance claims inherited from broad survey wording |
| TALENT standard random-split procedure | [TALENT v3 §§4.1–4.2](https://arxiv.org/html/2407.00956v3#S4) | Pinned result table identities `_sources_l058.json` | Paper says64/16/20,100trials,15seeds,accuracy/RMSE; local Markdown cannot certify these histories |
| Dataset count/version contradiction | §4 vsFigure3 caption; pinned `results/readme.md` |120binary/80multiclass/100regression counted, exact file hashes | Figure3 swaps binary/regression counts; release note links replacement data. Frozen historical evidence, not current leaderboard or validated raw-data identity |
| Within-row ranking and ties | §5.1; [official average_rank.py](https://github.com/LAMDA-Tabular/TALENT/blob/1b973adffa4203c3f62c4949957b1ba3efbe60d3/visualization/average_rank.py) | `rank_matrix`; `_source_check_l058_v2_results.json` | All300×6 rankentries agree exactly after explicit orientation; spreadsheet read supplied in memory; pandas3 applymap→map compatibility alias. Upstream missing-rank mean-imputation is not inherited |
| Preserve observed mean and dispersion | Raw three source tables | `parse_talent` returns aligned mean/SD frames | Explicit missing markers, scientific notation and emphasis tested; malformed values,negative SDs,duplicate IDs rejected; no invented seeds |
| Distinct tiny benchmark purposes | TALENT §8.1 vs§8.2 | Separate narrative and actual selector | Tree/DNN/Tie diagnostic is not “45 most favorable XGB tasks.” Original biased demonstration preserved as historical evidence |
| Rank-consistent subset objective | TALENT §8.2 detailed strategies and Eq5 | `select_tiny`: first minimum mean-rank MAE, unique row subsets | Source prose discusses averages vsEq5 sums; detailed descriptions/Table7 caption useMAE vsapplications paragraphMSE. Local objective declared, not exact equation parity |
| Evaluate methods excluded from selection | TALENT §8.2 applications | `method_holdout` reranks seen columns BEFORE selection; heldout mutation test | Avoids indirect leakage via all-model ranks; independent unseen ranks; exact original split/selector unavailable |
| Pairwise uncertainty | Derived paired-difference identity and explicit bootstrap extension | `dataset_bootstrap`; CatBoost-minus-RealMLP/TabR gaps |2000paired dataset draws,percentiles; no t-interval label, no raw-seed covariance, no equivalence inference |
| Meta-feature observations vscausal mechanism | TALENT §§6–7; earlier lesson051 controlled interventions | New broader-picture section, no predictive meta-model fitted | Associations propose interventions; they do not establish causal model-family explanations |

Additional pinned official code is in `labs/sources/l058/audit/{average_rank.py,train_model_classical.py,utils.py}` and `_sources_l058_audit.json`. The original source manifest, three raw tables, release note, original verifier/operator and `_verify_l058_results.json` remain unchanged.

## Actual experiment and results

1. **Six-method audit:** XGBoost,CatBoost,MLP,RealMLP,TabR,FT-T; all300 published task rows complete. CatBoost meanrank2.9083,RealMLP3.0217,TabR3.0283. CatBoost−RealMLP gap−0.1133,paired percentile95%[−0.408375,0.191667]; CatBoost−TabR−0.1200,[−0.450000,0.196750]. Intervals crosszero; no resolved ordering or equivalence conclusion. Dataset-relatedness and historical-protocol limits remain.
2. **Missingness-aware tiny audit:** the13-classifier/12-regressor paper-like family pool has historical TabPFN missing on4binary and20multiclass tasks. Thus116/60/100complete rows,276total, and rounded15% subsets17/9/15=41tasks, not original300/45. Every exclusion and selectedID is recorded.
3. **Held-out methods:** five declared cyclic rotations reserve3classification/2regression methods; three searchseeds58–60. Ranks are computed within seen/unseen pools separately. Some holdouts overlap and two regression methods are never held out. These45cases are sensitivity cases, not independent samples. Different pool sizes also make raw seen/unseenMAEs different scales.
4. **Measured optimization transfer:**1,000proposals gives15/45heldout regressions against the first random candidate,26improvements,fourties at1e−12.10,000gives20regressions,24improvements,onetie. Increasing budget worsens heldout fidelity23cases,improves13,ties9. More proposals improve the selection objective but need not transfer. Exact originalTable7 is not reproduced.
5. Both budgets ran locally; the closer recipe matches the paper's10,000randomproposal count. No cloud run, GPU, pretrained checkpoint or modeltraining was needed. Runtime measurements are incidental editor-load observations, not an efficiency comparison.

Fresh outputs: `labs/_verify_l058_v2_results.json`, `labs/_verify_l058_closer_results.json`. Both store actual current full-module SHA256,source revision and runtimeversions. Historical results retain their original operator identity; no old hash was refreshed merely to pass a checker.

## Teaching and implementation repair

- Canonical sources: expanded `lessons/content/0058-surveys-meta-benchmarks.md`, existing explicit depth blocks retained, scoped `labs/_build_l058.py`, `labs/relkit/talent_audit_l058.py`.
- Five live TODOs: `parse_talent`, `rank_matrix`, `dataset_bootstrap`, `select_tiny`, `method_holdout`. Each has goal/why/hint and immediate behavioral checks; the student notebook contains no completed versions. Completed functions are visible in the ignored teacher copy.
- Counting wrappers confirmed actual1000-candidate audit calls: parser3,ranker138,bootstrap2,selector45,holdout45. Teacher also completes the10,000track using the same live functions. No completed-code subprocess replaces student functions.
- New protocol image carries six-task ranks through candidate selection and held-out evaluation; interactive `tiny-selector` exposes the exact same values and first-minimum tie. `ranks-viz` retains the earlier task-denominator example.
- New measured figures: paired subgroup transitions and six-method percentile intervals. All distinguish synthetic fixtures,frozen author evidence and student outputs. Portable inlinePNG images and full-sizelinks are present in the notebook.
- Early static launcher provides run/download/read-onlypreview/exercises/reference/evidence/reproduction. No fetch is needed for058's own links. Parent owns shared directory/config,legacy generator delegation,workflow exports and browser/publication checks.
- EXIT saves sources,results,livecallcounts and a written argument; no personal mastery or resumability is claimed. Reference and reproduction instructions agree with the actual41-task closertrack.

## Validation performed

- `labs/_check_l058_v2.py`: PASS allfive contracts: realPima mean/SD,scientific notation/missingness/malformed cells/negativeSD/duplicate IDs; metricdirection/average ties; paired bootstrap and deterministic draws; first-minimum ties/unique proposals/nested objective; heldoutscore mutation does not change selection.
- `labs/_source_check_l058_v2.py`: PASS1800complete-panel rankentries,maximum delta0; declared pandas compatibility/IO changes. No upstream selector or training parity claim.
- `labs/_verify_l058_v2.py` and `--trials10000`: both RUN; currentoperator hashes checked against saved outputs.
- `labs/_execute_l058.py`: PASS19/19teacher codecells,including both searchbudgets. Current final recorded execution44.47seconds; not a runtimebenchmark. Blankstudent HTML regenerated; teacher remains ignored.
- Portable `protocol.png`,`tiny.png`,`rank-intervals.png` inspected visually at full image size. Computed trace and labels agree; figure wrappers have keyboard focus,scrollhints and full-size access.
- Parent independently reconstructed all300six-model ranks and90subsetcase errors/IDs from raw cells using a pairwise rank formula; combined report `058-ranks.json`. Parent also verified the original pinned remote files byte-for-byte in `058-sources.json`.
- Scoped `check_lesson(58)`: PASS;69 local links, matching depth prose, four valid portable images per notebook,19 clean executed teacher codecells and19 blank student codecells; saved as `058-delivery.json`. Browser/staging results are maintained by the parent. Live Colab and publication are separate claims; no liveColab check is asserted here.

## Residual fidelity gaps

Historical released means may refer to different dataset/protocol versions than the corrected v3benchmark; raw arrays,tuning traces and pairedseed data are absent here.13/12method completeness removes24tasks; local tinysets have41tasks. Exact paper methodsplits,selectedtask membership and original selector are not recovered. Within-group ranks and declaredMAE resolve ambiguities transparently but are not proven authors' exact implementation. TinyBenchmark1,learningcurve prediction,heterogeneity featureselection,fullmodeltraining and Table7 reproduction remain unrun. Friedman/Nemenyi were not run or substituted for the paper's different Wilcoxon-Holm protocol. The absence of raw seeds prevents recovery of paired training-seed uncertainty; dataset-level ranking itself does not require those raw seeds. The bootstrap is a transparent local sensitivity analysis.


## Integration delivery checks (performed by parent)

- `058-browser.json`: PASS at 900px and 375px for `ranks-viz`, `tiny-selector`, and `protocol-overview`; no page errors, missing resources or reported layout problems. A detected narrow-screen rank-table overflow was repaired with a lesson-scoped focusable horizontal scroll region and visible hint.
- `058-access.json`: PASS actual no-JavaScript lesson → preview → exercises and gallery → directory → preview routes at both widths; failed-manifest-fetch gallery route also passed. Downloaded notebook bytes matched. Colab destination was inspected but the live Colab UI was not run.
- `058-pages.json`: PASS 120 links and HTML fragments in a real copied Pages staging tree, with zero broken targets. This establishes local staging; post-push publication remains a parent-owned separate check.

## Parent integration

The generic lesson builder and notebook builder now delegate lesson 058 to its scoped canonical builder. Regenerating through `--lesson 58 --keep-notebooks` preserved the exact canonical lesson, reference, preview, student, executed teacher and directory bytes (`058-regeneration.json`). The shared directory card now lists all five actual tasks, the current EXIT path, and both new evidence artifacts; other directory cards were preserved byte-for-byte. Required JSON files are included in Pages exports.

Root browser review found and fixed mobile overflow in the legacy rank widget with a scoped accessible scroll region. Final browser checks passed at 900/375 pixels. Native keyboard budget changes reproduce (.50, .00) at one proposal and (.00, .50) at two/three, retaining the first minimum; both widgets reset correctly (`058-viz.json`). Full copied Pages passed 120 local links. Aggregate coherence across lessons 047–058 passed.

Publication recovery: lesson 057 commit `f80b153` built successfully, but GitHub Pages timed out in `updating_pages` and canceled that SHA deployment. The publication checker can now verify unchanged lesson bytes against their original commit after a successful later deployment, recording the two SHAs separately. This does not assert that the timed-out deployment succeeded.
