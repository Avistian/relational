# Lesson 065 — query embeddings with a full pretrained v2 model

## Verdict

Before: the lesson correctly named an information-role problem, but its active student exercise used a random reduced encoder and one fold-scatter TODO. The author evidence used three folds, final-layer vectors and averaged evaluation contexts. That package could not teach or execute the paper's complete layer-selection mechanism, and its small implementation was disconnected from the full historical pretrained v2.

After: the canonical package uses the unchanged full L064 v2 implementation/checkpoint, inlines the whole model in the notebook, and adds four live extraction/scatter/concatenation/validation-selection tasks. It implements ten-fold query extraction with all 12 intermediate target states and 894 layer/C candidates, with a real logistic head and complete small datasets. The central paper mechanism is now runnable. Original 29-task results are not reproduced; exhaustive search and full-training evaluation context are explicit local protocol completions.

## Baseline and sources inspected

Read the audit brief and mission, relevant NOTES delivery/quality preferences, teach, lesson-pedagogy, lab-authoring and lesson-visuals skills. Compared L042's explicit baseline/fair-protocol presentation, L043's visible sparsemax/attention tasks and source checks, L044's routing operation and baseline experiment, and L045's annotated implementation/real-data/pretraining boundary. The repair follows their useful standard: visible operations, meaningful learner work, actual experiments and honest conclusion scope. No learner mastery or mission edits were made.

Primary paper: Ye, Liu and Chao, [arXiv 2502.17361v1](https://arxiv.org/html/2502.17361v1). Read all §6, Figure 5, Table 2, Appendix C Table 10, relevant §4.1, §5, all §7 and Appendix D. The declared scope is version 1; later revisions were not comprehensively audited. Targeted searches did not establish an original Table 10 experiment repository. [Current official embedding docs](https://docs.priorlabs.ai/capabilities/embeddings) provide a later fold/full-training-context comparison, not original experiment provenance.

Implementation: pinned 2.0.9 wheel, `utils.py::_get_embeddings`, `model/transformer.py` target extraction, all 12 original block outputs, numeric preprocessing configuration. Existing L064 core SHA remains `6f50292d3cb41597e217a4e6a7f71eb6bc120189f6295bd5975a7ec8dc75e747`; checkpoint SHA `f65a35685aeef42e31b796d9bfa34e68d6fc780bc98e7bff7763802964cf435f`.

## Claim → implementation → evidence inventory

| Claim / obligation | Primary source or derivation | Actual code | Evidence / limit |
|---|---|---|---|
| Context target states and query target states have different available labels | §6.1; release target encoder | `extract_target_states`, unchanged L064 target encoder | Fixed-context own-label query delta 0; context label intervention changes all layers |
| Correct extraction selects target token after complete block | Release transformer `_forward`; §6 | Hooks in `extract_embeddings`; Q slice and target-token slice | Every layer compared with original source hooks; final states separately compared with public API |
| Frozen parameters do not remove context-label conditioning | §6.1 mechanism | Full pretrained model supplied observed context labels | Source fixture label intervention; no random substitute |
| Ten-fold extraction excludes own fold | §6.2 | `scatter_fold_embeddings`, ten stratified folds | Original IDs/context/query trace in every measured run; singleton/misaligned cases taught |
| Fold output concatenation can scramble targets | Derived identity-coded example | Scatter by query positions | CHECK restores six identity-coded rows, verifies no own context overlap and one write per row |
| Layers 6/9/12 and up to three of twelve layer choices | §6.3/Table 2/Table 10 | `concatenate_layers`, complete 298 subset enumeration | 894 validation candidate rows; original search details unspecified |
| Validation must select, with test untouched | §4.1 plus local stated selection rule | `choose_candidate`, `fit_candidates` | Accuracy-only comparator; deterministic ties; class axes/coefficients/scalers/probabilities saved |
| A useful probe is a complete fitted pipeline | Logistic likelihood/L2 derivation | Visible `fit_head`, train StandardScaler, liblinear logistic solver | Raw/vanilla/fixed/combined/native arms use same outer rows; solver is peripheral PROVIDED code |
| Query role alignment does not guarantee same context distribution | Derived from local support policy | 90% support for training vectors, all training support for evaluation | Clearly labeled local completion; no averaged-context claim |
| Correct mask does not imply wrapper query independence | Original L064 encoder counterexample | Query batch fixed for label intervention | L064 evidence linked/explained; batch/query ID ledger retained |
| Paper schematic tokens differ from actual grouped release | §5 Eq. 2 versus release | Unchanged additive group projection | Parent `_token_identity_l065_results.json`; zero-value low-level fixture scope stated |
| Combined is not a universal winner | Table 10 | Parent paper-table parser | 16 wins/4 ties/9 losses vs native; rounded rank differences not called paper errors |
| Live code/source/weights must agree | Evidence audit obligation | Recursive nested-code identity, defaults/model methods, loader SPECS/CACHE, constants, versions, tensor digest | Source checker tests changed selector inside comprehension; notebook rejects stale source identity and edited weights/checker files |

`labs/_sources_l065_v2.json` records the source/code map and file digests. The original paper's 29-task pool is Tiny benchmark 2, not the separate Nature-paper roster.

## Repairs

- Replaced the short canonical manuscript/depth overlay with a connected standalone lesson: role inputs, tensor axes, ten-fold information flow, identity-coded scatter, source API behavior, linear-head objective/scaling, layer-search combinatorics, validation/test discipline, native/raw/vanilla comparisons, failure diagnosis and relational transfer.
- Added an end-to-end pipeline diagram with actual tensor slicing and native/probe branches. Separate figures expose target ranks/flags and measured role intervention, scatter destinations, selection logic and per-seed measured scores. Full-size inspection and accessible horizontal scrolling preserve readability.
- Added two actual computational widgets with distinct mount/figure IDs. The fold widget changes held-out membership and shows deliberate concatenate corruption. The selection widget changes validation/test scores independently; test never chooses a candidate. Reset and keyboard-native controls are supported.
- Added a dedicated scoped builder with requested APIs. It renders student, ignored teacher, prepared preview, reference and lesson from canonical sources, preserving teacher execution only when code is identical. Removed obsolete depth overlay. Parent owns shared routing/access/index/assets/publication.
- The full model is PROVIDED in coherent annotated chunks; four actual TODO functions drive the experiment. The notebook has a portable overview, independent source checker, diagnostic CHECKs, prediction before measurement, a complete pretrained diabetes run, identity-bound EXIT and optional broader local preset.
- Historical `_verify_l065.py`, `_verify_l065_results.json`, reduced operators and old figures remain unchanged. New evidence is versioned. No cloud/paid job, child agent, commit or push was launched by this author.

## Checks performed

- `_check_l065_v2.py`: PASS against actual 2.0.9 source. Query target states across all 12 layers maximum absolute delta 1.0073e-5; context states 9.5367e-6. Public final-layer get_embeddings agrees. Fixed-exclusion own-label delta exactly 0; context-role final-layer delta 0.51064.
- Teacher notebook: all 38 code cells executed locally with no errors, source checks, complete 768-row diabetes seed-7 experiment, all 894 candidates and EXIT. Selected head and native both achieved test accuracy 0.7792208 on that split. Execution record in `_execution_l065_v2_results.json`; ignored teacher solution remains fully executed.
- `_browser_quality_audit.py 65 --widgets l065-scatter-widget l065-selection-widget --architecture l065-architecture`: PASS, no console/request/geometry/page overflow errors at 900 and 375 pixels. Exercised all select choices, checkbox toggle/keyboard restore, slider endpoints/keyboard and reset; screenshots inspected. Native scroll regions retain portable diagrams; figures have full-size links.
- Fresh `_verify_l065_v2_results.json`: nine complete dataset/seed runs, every layer captured in ten folds, all 894 distinct layer/C candidates in each run. Author source-check and measurement kernel hashes match. Combined vs native: 0 wins, 1 tie, 8 losses; all vanilla heads have fitted training accuracy 1.0. This negative result is taught alongside the raw, fixed-layer and native baselines.
- `_identity_l065_v2_results.json`: PASS for positional/keyword defaults, protocol, live loader SPECS/CACHE, model method, actual tensor mutation and fresh-process identity stability. Every temporary mutation was restored. The changed nested selector is separately tested in the source checker and live notebook.
- `check_lesson(65, require_review=True)`: PASS, 38 code cells in both notebooks, six inline portable figures, executed teacher and blank/unexecuted student, all current local links, prose and preview consistency. Saved scoped result `065-delivery.json`; aggregate delivery untouched.
- Final six figures regenerated from actual source/measurement artifacts; no in-progress placeholder remains. Long role ylabel shortened after visual inspection. Three-panel plots retain repeated row labels and a minimum 1100-pixel scrollable reading width; roles use 900 pixels and architecture/scatter/selection 720 pixels. Source images and mobile widget screenshots inspected. Pipeline boxes have bounds/non-overlap assertions.
- New verifier `--output` uses exclusive creation and refuses existing evidence; refusal was tested before any run starts. The completed author measurement predates only this output-destination/overwrite-guard change; all model/extraction/selection operators and measured identities remain unchanged.

## Remaining evidence limits

Full 29-dataset paper benchmark **NOT_RUN / INCOMPARABLE**. Dataset versions, split-seed definitions, original layer search/head settings, original evaluation context and wrapper protocol are not established as exact Table 10 matches. Current results use a selected binary numeric lane, three seeds, an explicit one-view numeric recipe and local exhaustive search. There is no original synthetic retraining, cached/uncached equivalence claim, all-categorical-path proof, live Colab verification or author publication claim. Parent separately owns staged delivery and public deployment checks.

## Parent independent review and delivery

The parent read the complete canonical lesson/lab implementation and checked the primary paper's §6 and Appendix Table 10 independently. `_paper_audit_l065.py` parses all 29 rows from version 1, preserves published ranks, reconstructs average-tie ranks from rounded scores, and computes bounds for unknown ordering within printed ties. Those bounds are compatible with the published ranks; the unpublished precision and exact ranking procedure are not established. Combined versus native is 16 wins, 4 ties, 9 losses in the frozen paper table. This is separate from the nine fresh local runs.

`_token_identity_l065.py` runs the historical low-level encoder on zero-valued groups. Its first-block inputs equal the additive projected group identities exactly (norm 7.3548532), exposing why the paper's multiplicative schematic cannot replace the release operator. This is a low-level fixture, not a claim that the raw constant-filtering wrapper retains zero columns.

`_independent_l065.py` imports the original 2.0.9 network, not either teaching model. It reconstructs raw data and split/fold identities, re-extracts all ten folds and validation/test states from all twelve original layers, verifies all 894 candidate identities and validation-only tie selection, reconstructs selected scaler/head probabilities, checks the regularized liblinear objective's gradient residual and independently calculates every saved test metric. All **8,778 author predictions** and **1,078 student predictions** pass. The largest author relative head-gradient residual is 0.0001226. Reports: `065-independent.json` and `065-student-independent.json`.

The generic entrypoint executed the full lab through the explicit smoke-to-lab mapping. All head records and probabilities match the teacher run exactly. Module identity matches the author module identity; notebook compilation and class namespaces have a separate identity, preserved rather than relabeled. `065-runner.json` records this distinction.

The parent independently checked 138 actual displayed widget states at 900 and 375 pixels, including every validation slider value, three independent test outcomes, all fold destinations, intentional concatenation, keyboard and reset. Source PNGs and mobile screenshots were inspected; clipped labels and an ambiguous architecture branch were fixed. The final architecture distinguishes validation/test extraction, training-only transforms and native temperature. Six artifacts survive generic regeneration byte for byte. The final real Pages copy has 118 valid local links/anchors; no-JavaScript lesson/preview/exercises and gallery/directory routes, download bytes and failed-manifest fallback pass. Browser, copied staging and live publication remain separate evidence; live Colab was not run.
