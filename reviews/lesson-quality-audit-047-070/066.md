# Lesson 066 — complete original-checkpoint TabICL audit and repair

**Before: substantial repair required.** The prior package had a short architecture overview and expanded prose overlay, but its two live tasks implemented only untrained inducing attention and affine arithmetic. Actual scores came from a packaged later `v1.1-0506` model. The notebook did not expose the complete model that produced those scores, and the scale experiment was confined to a single dataset. The original-paper checkpoint/version distinction and exact MAB/normalization/RoPE/head/wrapper path were insufficiently explicit.

**After: scoped authoring complete.** The repaired lesson implements all original pretrained layers visibly, binds five student functions to actual measurements, uses the original February checkpoint, and includes extensive connected paper/source explanation, eight portable computational figures, three live numerical widgets, complete small-data experiments, original-source checks and a fully executed teacher notebook. Original synthetic pretraining, full published benchmark and 500K resource reproduction remain NOT_RUN. The overall paper-result verdict is INCOMPARABLE. Parent owns independent delivery, integration and publication checks; this author did not commit or push.

## Sources read and version decisions

Primary paper: [Qu et al., arXiv2502.05564v1](https://arxiv.org/html/2502.05564v1), full central §§3.1–3.4, §§4.1–4.4, §5 and Appendices B/C/D/E, including the curriculum, tree SCM generation, generic versus model-specific RoPE bases, benchmark split/ensemble/time-accounting and CPU/GPU/disk memory boundaries. Reviewed original lesson/canonical/depth material, reference, full notebook source, generic builder/runner, old reduced helpers/evidence; read MISSION, user delivery and paper-mirror preferences, teach/pedagogy/lab/visual skills and representative baseline lesson/lab material.

Pinned original-source package: [tabicl 0.1.4 wheel](https://files.pythonhosted.org/packages/c9/56/bbac0d5076acbca1075177c0a40c829a497fc26598fc90fa55c73d38288a/tabicl-0.1.4-py3-none-any.whl), SHA `10dcaec46e9e2ba37adb3c1c386f53253f210bbc2894af2e6d0506131c0abdcc`. The scoped source tree includes attention, layers, encoders, embedding, interaction, learning, RoPE, inference manager/config and sklearn classifier/preprocessing; source/dep hashes and BSD license retained under `labs/sources/l066-v2` and `labs/_sources_l066_v2.json`.

Active original checkpoint: [v1-0208 at immutable revision eaf789…](https://huggingface.co/jingang/TabICL-clf/resolve/eaf789a9b25ee8486d6f48997ba076f850bbc30b/tabicl-classifier-v1-0208.ckpt), SHA `f5bae1d31181a1bb4ab8e97d2a5e62a504e856f23b4ea62c7fcc2f8eec4995b6`, 108,313,922 bytes. The original classifier `_load_model` explicitly distinguishes this as the paper version from later `v1.1-0506`. The parent downloaded/hashed the original asset; the repaired loader verifies that checksum before strict loading. Historical files/results and later checkpoint remain unchanged.

Important corrections: §3.3 explicitly uses RoPE base 100000; Appendix C's 10000 is generic and is not an architecture mismatch. Appendix E's 132 small+56 large gives 188 supported-class datasets; introduction 55 large wording is inconsistent. Appendix D 500K×500 experiment is 80/20 context/query, <14 GB GPU but about 120 GB CPU before optional disk offload. Neither this eager implementation nor a GPU-size number alone establishes the paper resource claim.

## Claim → paper/source → implemented path → checked evidence

| Load-bearing item | Primary/source anchor | Implementation | Evidence and boundary |
|---|---|---|---|
| Two representation phases, three transformers | §3.1/Figure 1; `model/tabicl.py` | `TabICL`, `ColumnEmbedding`, `RowInteraction`, `ICLPredictor` | Full copied-stage/model checks; exact config and all 277 state tensors loaded |
| Shared scalar projection transfers across schemas | §3.2/Eq 3; `embedding.py::_compute_embeddings` | `SkipLinear`, `ColumnEmbedding` | Complete column outputs on three fixtures; no semantic-name or causal-identification claim |
| Three ISABs,128 inducing readers,4 heads | §3.2/Eqs 4–5; `layers.py::InducedSelfAttentionBlock` | `Encoder`, `InducedBlock`, live `inducing_memory` | Whole-column stage parity; synthetic two-reader arithmetic; context-only memory and query interventions |
| MAB includes pre-LN, residuals and FFN | Figure 2; `MultiheadAttentionBlock.forward` | `PackedAttention`, `AttentionBlock`, live `attention_mix` | All stage parity plus independent NumPy/SciPy reconstruction; no convex-hull claim about full block output |
| Generated W/B normalized separately | Eq 1/2/6; `embedding.py` | live `conditional_affine` and separate LN heads | Original copied-column output parity; normalized-head CHECK; synthetic final-affine arithmetic |
| Distribution information is empirical, not exact named statistics | §3.2/Figure 3 | Explanation distinguishes summary PCA from exact statistics | Cited 40K-column visualization; no local Figure 3 reproduction |
| Four CLS outputs retain separate coordinate slots | §3.3; `interaction.py::_aggregate_embeddings` | `RowInteraction`: four normalized 128-wide CLS outputs concatenated to 512 | Whole row-stage source parity; architecture shape trace |
| Adjacent-pair row Q/K RoPE | §3.3/Appendix C; `rope.py`, `attention.py` | live `rotary_pairs`,8 fixed frequencies,16-wide heads | Pair-length/angle CHECK; full row-stage parity; synthetic dot-product widget |
| Symmetry collapse with identical marginals | §3.3/Figure 4 | Explanation plus parent original-source fixture | `labs/_symmetry_l066_results.json`: fixed weights RoPE on/off; not retrained Figure 4 reproduction |
| Labels enter only in ICL | §3.1/3.4; `learning.py::_icl_predictions` | `ICLPredictor` onehot→512, context-only addition | Original-source label flip leaves pre-ICL rows exactly fixed and changes logits; link to 065 role distinction |
| Every ICL reader sees only context keys | §3.4; `attention.py` integer-mask path | live `context_keys` in each of 12 blocks | Full logits parity; query mutation/appending invariance under supported wrapper; role matrix |
| Complete head and class/temperature handling | §3.4; `learning.py`, `classifier.py::predict_proba` | LN 512→Linear 1024→GELU→Linear 10, firstK, temperature.9 | Complete output/source-wrapper probability parity; active 2–10 class support stated |
| None normalization still scales and softens outliers | `sklearn/preprocessing.py` | `fit_numeric`, `transform_numeric` | Original-wrapper transformed-array comparison with constant and extreme values; finite numeric scope |
| Optional power fallback can couple queries | `PreprocessingPipeline.transform` | Explicit outside-scope caution | `labs/_query_coupling_l066_results.json`, parent source/runtime-specific counterexample; no overgeneralized 32 view isolation claim |
| ICL remains context-quadratic | §3/§4.4/Appendix D; original attention layout | Column 12Fm(C+N),row 24N(F+4)²,ICL 48NC | Generated cost curve/widget and arithmetic fixture; score counts distinguished from memory/time |
| FlashAttention/batching/offload permit different memory behavior | §4.4/Appendix D.2 | Explained, not implemented in eager numeric model | Original 100K/500K resource experiments remain NOT_RUN; CPU/disk demand explicit |
| Synthetic prior and curriculum create the copied weights | §4.1–4.2/Appendix B/D.1 | Detailed SCM/tree/function/curriculum explanation | Actual original training NOT_RUN; no tiny substitute mislabeled as pretraining |
| Many-class probabilities form a product along a hierarchy | §4.3; `learning.py` hierarchy | Derivation with .6×.25=.15, shared label-free rows | K>10 implementation/evaluation excluded and NOT_RUN |
| Large benchmark comparisons need paper protocol | §5/Appendix E | Explicit 188/200 roster,64/16/20,32 views, time-accounting caveats | Local experiment cannot reproduce published family ranks/speedups/calibration claims |
| Larger context need not improve every metric | Local paired intervention, grounded by complete model | `run_experiment` with five live functions | 27 runs/3 datasets/3seeds,3762 query predictions; paired loss/accuracy curves and exception cases |

## Repairs and standalone teaching value

`lessons/content/0066-tabicl-column-row-attention.md` is now the complete connected source. The obsolete `lessons/depth/0066.md` overlay was removed, not emptied. It defines axes and terms, derives attention/MAB/inducing/RoPE/mask/cost computations, provides coherent numerical fixtures, connects to TabPFN v1/v2 and 065 representations, and separates original-paper claims from local scope. The `l066-architecture` figure identifies the precise original-checkpoint variant and full path.

`labs/relkit/tabicl_l066_v2.py` contains the complete checked inference architecture; the notebook inlines coherent code chunks rather than hiding the model behind a package import. Five TODOs: `attention_mix`, `context_keys`, `rotary_pairs`, `inducing_memory`, `conditional_affine`. Each has goal/why/hint, immediate diagnostic arithmetic/shape checks, and live use in actual scored stages. Source checks receive the current notebook namespace; student functions cannot be bypassed by switching to a packaged measured model.

Eight generated PNGs expose architecture, both inducing softmax matrices, RoPE rotation/dot products, role mask, all-stage score counts, actual paired loss/accuracy, exploratory ranks/stage timing, and the fixed-weight symmetry intervention. Every PNG was opened and visually inspected; the architecture label route was corrected. They carry full-size links and inline data-image payloads in both notebooks. Widgets `l066-inducing-viz`, `l066-rope-viz`, `l066-cost-viz` have separate causal questions, native sliders/reset controls and preserved baseline values. Parent checked 102 displayed states at 900/375 including keyboard behavior.

`labs/_build_l066.py` owns `build_package(notebooks=True,render=True)`, `build(solution=False)`, `render_preview()`. Student/solution prose is identical; solution outputs survive regeneration only when executable cells are byte-identical. Preview launcher appears before body, says read-only, links executable notebook/Colab, reference/evidence/reproduction and `#lab-exercises`. Lesson/reference/prepared tables have explicit horizontal scroll and unbroken numeric/header tokens. Parent owns shared generic delegations, directory/access/TASKS/PANELS/export wiring.

## Executed checks and measurements

- `labs/_check_l066_v2.py`: PASS using original source in an isolated process and actual copied weights. Three nontrivial fixtures cover 2/3/10 classes and multiple row/feature counts. Maximum absolute full-stage difference `5.185604095458984e-06`; wrapper probability maximum difference `1.1920928955078125e-06`. Complete stage tolerance 3e-4; wrapper 2e-5. Transformed numeric inputs match within 1e-6. Query mutation/append checks pass.
- `labs/_verify_l066_v2.py`: 27 fresh complete-model inference measurements, all data rows, three nested sizes and three seeds on diabetes/blood_transfusion/WDBC. Original checkpoint; float32; one CPU thread. No overwrite/resume. An initial run used the unsupported alias `blood` and failed before writing any completed result; the canonical key was repaired and the entire panel rerun fresh. No partial output was relabeled.
- `labs/_analyze_l066_v2.py`: reconstructs metrics from saved probabilities, checks disjoint IDs/full largest partition/nested contexts/fixed queries, and recomputes means/SDs, paired t 95 gaps, dataset-balanced ranks, Friedman/Nemenyi and 216-permutation null. Complete input payload SHA recorded. Inferential analysis requires at least three datasets/seeds; smaller runs remain descriptive.
- Means of log loss small/middle/full: diabetes `.50210/.46658/.45853`; blood `.56721/.49301/.47125`; WDBC `.07366/.06575/.04734`. WDBC mean accuracy reverses at middle context. Full-minus-small paired t 95 intervals all include zero. Ranks 3/2/1; Friedman p≈.0498; exact exchangeable-rank permutation p≈.0278; CD 1.914. Both significance summaries are explicitly fragile/exploratory on this three-convenience-dataset panel.
- `labs/_execute_l066.py`: all 41 teacher code cells executed in IPython in-process, PASS, 11.30 seconds including live source check and original-checkpoint diabetes measurement. Report `labs/_execution_l066_v2_results.json`. Student has five blank model TODOs plus blank written interpretation; teacher EXIT states actual paired values, a surprise and a concrete follow-up. No student mastery inferred.
- Current source check/module experiment identities agree; current canonical module SHA equals the fresh measurement's source SHA. Notebook identity is retained separately and its aligned predictions match the module evidence numerically. The notebook tests a changed global helper called inside a generator expression and a changed actual checkpoint parameter; both invalidate evidence, then restore cleanly. EXIT checks source-worker/manifest hashes and writes fresh with exclusive creation.
- Parent independent evidence: `066-independent.json` reconstructs the full model with NumPy/SciPy; parent reports maximum float64 source-logit difference 7.46e-14. `066-evidence.json` reconstructs all 27 records/3762 predictions against original source and raw data. `066-runner.json` checks generic smoke routing and byte-exact aligned seed 7 probabilities/logits. `066-widget.json` checks all 102 widget states. These are parent-owned independent checks, not author-generated claims of paper-result parity.

## Remaining boundaries

No original synthetic pretraining, full 188-supported/12-many-class TALENT benchmark,32 view ensemble procedure, many-class hierarchy inference, or 500K resource measurement was run. Visible eager attention materializes scores and is not the original memory-managed implementation. Missing/categorical arrays and all-constant contexts are rejected by the supported wrapper. Original source 0.1.4 was checked under the recorded modern local torch/numpy/sklearn runtime; package-source parity is not historical runtime equivalence.

Live Colab execution and cloud execution: NOT_CHECKED/NOT_RUN. Parent owns final browser/Pages staging/links/publication and will record those independently. Mission and mastery files were not changed. Author files are ready for parent delivery review; no commits/pushes performed by this author.

## Final runtime-identity hardening

The author found that class-code and tensor hashes alone could miss instance-level forward overrides or a changed runtime normalization epsilon. Added `model_runtime_identity`: every actual module class/forward and public runtime setting is bound; instance overrides and forward hooks are rejected. Source CHECK, experiment and EXIT now agree on a separate `runtime_sha256`. `_identity_l066_v2.py` confirms changed epsilon and weights invalidate evidence, an overridden forward rejects certification, and restoring the model restores its runtime identity. The same checks execute in the teacher notebook.

After this change the complete 27-run measurement was rerun fresh, then source checks, analyses, figures and all 41 teacher cells were rerun. Numerical probabilities/logits exactly match the provisional earlier measurement; fresh stage timing is 9.81 seconds and teacher execution 11.30 seconds. The provisional operator and measurements remain in ignored `labs/data/cache/l066-pre-runtime-binding`; no historical foundation evidence was rehashed. Final canonical core SHA256: `64ae4a600f85069aef324452ea7d8479610a0d56b1c57e878e0308cf9cdf7ff5`.

Final author delivery check `check_lesson(66,require_review=True)` passes: 87 local links, 41 code cells in each notebook, eight portable images, blank student/fully executed teacher, matching canonical and prepared prose. Parent publication/browser/access checks remain separately owned.

## Parent independent acceptance and delivery

Parent rechecked the full paper and original source, independently reconstructed all pretrained stages in NumPy/SciPy, and reran all original-wrapper predictions after final runtime hardening: 27 author records / 3,762 predictions and three teacher records / 462 predictions PASS. Final author payload binding, paired confidence intervals and exact 6/216 permutation calculation agree. The generic smoke route was rerun using the final operator and exactly matches the three corresponding author probability/logit arrays. Runtime and kernel identities agree.

Final architecture PNG was viewed; the context-label connector clears all shape labels. The mobile figure is an explicitly labeled, keyboard-focusable horizontal scroll region with a full-size link. Actual widgets pass 102 independently recomputed numerical states; browser geometry, reset and keyboard checks pass at 900/375. No-JavaScript lesson-to-preview-to-exercises and gallery-to-directory paths, actual notebook downloads, and failed-manifest behavior pass. Real copied Pages staging verifies 133 local file/anchor links. Generic regeneration leaves lesson, reference, student, executed teacher, prepared HTML and directory byte-identical. Aggregate package consistency passes lessons 047–066.

Teacher was executed under an explicitly fresh `L066_OUTPUT` directory; parent checked the resulting `labs/data/cache/l066-teacher/1789211895614709183/exit-v2.json`. The ordinary student default remains `labs/data/cache/l066-student/exit-v2.json`. No live Colab execution or full paper benchmark is claimed. Publication verification follows the individual commit.
