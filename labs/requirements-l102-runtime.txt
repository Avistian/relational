# Pinned complete GPU experiment runtime (Modal Python 3.12).
torch==2.8.0
numpy==2.2.6
pandas==2.3.2
scikit-learn==1.7.1
# CPU authoring/teaching runtime is recorded separately in the evidence JSON.
