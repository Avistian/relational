# Optional: upstream parity and full FastText cache regeneration only.
# The student notebook uses the standard course dependencies and compact cache.
torch-geometric==2.8.0.post1
fasttext-wheel==0.9.2
