RetrievalBank.mount(document.getElementById('l099-warmup'),{upTo:99,count:3});
Predict.mount(document.getElementById('l099-predict'),{prompt:'Remove learned HGT attention scores and retrain. Does the resulting model become R-GCN?',options:[{label:'Yes, all operators coincide',value:'yes'},{label:'No, other operators differ',value:'no'},{label:'Only the labels decide',value:'labels'}],correct:'no',reveal:'Uniform HGT keeps typed value/output projections, residual gates and LayerNorm. It normalizes across all incoming edges; R-GCN sums separate relation means. The worked trace gives 2 versus 1 even without these additional operators.'});
Teachback.mount(document.getElementById('l099-teachback'),{prompt:'An HGT run beats R-GCN. What evidence would let you attribute that gap to attention?',points:['Hold the graph, split and selection fixed','Intervene within HGT on learned scoring','Report changed capacity and optimization','Restrict the claim to the tested protocol'],model:'A family gap bundles scoring, projections, residuals, normalization and optimization. A paired uniform-attention ablation narrows the question by keeping shared operators and initialization. It still removes parameters and changes training, so one graph supports only a conditional mechanism claim.'});
