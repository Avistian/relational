(function(){'use strict';
 const warm=document.getElementById('l113-warmup');if(warm&&window.RetrievalBank)RetrievalBank.mount(warm,{upTo:113,count:3});
 const pred=document.getElementById('l113-predict');if(pred&&window.Predict)Predict.mount(pred,{prompt:'Two label passes, four induced batches per pass: how many optimizer steps does the mini-batch arm take?',options:[{label:'Two total optimizer steps',value:'two'},{label:'Eight total optimizer steps',value:'eight'}],correct:'eight',reveal:'Eight. The full-graph arm takes two. Equal label passes do not hold the optimization trajectory fixed.'});
 const teach=document.getElementById('l113-teachback');if(teach&&window.Teachback)Teachback.mount(teach,{prompt:'Explain why receiver-chunked layerwise inference is exact while isolated-subgraph inference can change predictions.',points:['Every incoming neighbor remains available.','Each layer reads a fixed previous-layer matrix.','All current-layer rows are completed before the next layer begins.','Dropout is off and global node order is preserved.'],model:'Chunking receivers changes when outputs are computed, while preserving every dependency. All nodes in a layer read the same completed previous-layer features. Independent subgraph inference removes boundary dependencies. Completing all layers on one chunk before the rest therefore computes a different graph operation.'});
})();
