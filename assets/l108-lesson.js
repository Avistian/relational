RetrievalBank.mount(document.getElementById('warmup'),{upTo:108,count:3});
TemporalSamplingViz.mount(document.getElementById('l108-sampler'));
Predict.mount(document.getElementById('l108-predict'),{prompt:'Keep all weights and questions fixed. Does a smaller fanout guarantee the same score?',options:[{label:'The score must match',value:'same'},{label:'The score may change',value:'change'}],correct:'change',reveal:'A faster implementation can preserve sampled events exactly. A smaller fanout changes the sampled context and recursive computation, so accuracy must be measured again. A recent window is another information intervention.'});
Teachback.mount(document.getElementById('l108-teachback'),{prompt:'Explain a fast, legal two-hop query and defend its speed/accuracy comparison.',points:['Use a half-open time interval','Pass the traversed edge time into the next hop','Separate fixed-sample optimization from changed context','Pair events and negatives, synchronize GPU timing','Distinguish checkpoint replay from retraining'],model:'For a query at time 8 and width 5, I search timestamps in [3,8). If I traverse an edge at 3, its child query uses cutoff 3. I can batch binary searches without changing the sampled records, which permits an exact equality check. Changing fanout or a recency window changes the information the frozen model sees. I keep positive questions, negative candidates and metric aggregation fixed and measure inference with synchronized GPU timing. These results describe fixed-weight inference; training with the new sampler could produce different results.'});
