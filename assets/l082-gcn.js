RetrievalBank.mount(document.getElementById('warmup'),{upTo:81,count:3});
Predict.mount(document.getElementById('predict'),{prompt:'On A—B—C with values [2,4,8], what is B after one GCN layer with W=1?',options:[{label:'Approximately five point four',value:'correct'},{label:'Approximately four point seven',value:'mean'},{label:'Approximately seven point zero',value:'sum'}],correct:'correct',reveal:'B = 2/√6 + 4/3 + 8/√6 = 5.415816. Both endpoint degrees matter.'});
MessagePassingViz.mount(document.getElementById('normalized'),{mode:'normalization'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Explain how test-node features can affect training while test labels remain held out. Then explain why GCN is not an ordinary mean.',points:['Transductive graph and feature access','Training labels alone determine the supervised objective','Validation controls stopping; test labels only score','Self-loops precede degrees; both endpoint degrees weight messages'],model:'The split limits which labels supervise training, not which nodes participate in routing. Neighbor features affect a labeled node and its gradient. Validation labels control stopping; test labels only evaluate the final model. GCN weights use both augmented endpoint degrees, so rows need not sum to one.'});
