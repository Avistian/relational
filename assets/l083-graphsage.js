/* Defaults: neighbor [5,1] gives mean [3,2]; fanouts 10,25 give 261 occurrences/root. */
RetrievalBank.mount(document.getElementById('warmup'),{upTo:82,count:3});
Predict.mount(document.getElementById('predict'),{prompt:'Reverse the order of the two neighbors. What happens to the mean-aggregator output?',options:[{label:'It remains exactly unchanged',value:'same'},{label:'It reverses coordinate order',value:'reverse'},{label:'It doubles every coordinate',value:'double'}],correct:'same',reveal:'The mean is symmetric in neighbor order. Sampling a different multiset can change it; permuting the same multiset cannot.'});
(function(){
 const root=document.getElementById('mean');root.innerHTML='<p><strong>Predict first:</strong> increase the first coordinate of the second neighbor. Which branch changes?</p><label>Second neighbor, first coordinate <input type="range" min="1" max="9" value="5" step="1"></label><button type="button">Reset</button><p>Baseline: self [2,4], neighbors [1,3], [5,1] → [2,4 | 3,2]. Identity transforms.</p><output aria-live="polite"></output>';
 const input=root.querySelector('input'),output=root.querySelector('output');function draw(){const x=Number(input.value);output.textContent=`Changed neighbor: [${x},1]. Mean: [${(1+x)/2},2]. Output: [2,4 | ${(1+x)/2},2]. Self branch remains [2,4].`;}
 input.addEventListener('input',draw);root.querySelector('button').onclick=()=>{input.value=5;draw();};draw();
 const budget=document.getElementById('budget');budget.innerHTML='<p><strong>Predict first:</strong> double the outer fanout. Does total work double?</p><label>First hop <input name="first" type="range" min="1" max="25" value="10"></label> <label>Second hop <input name="second" type="range" min="1" max="25" value="25"></label> <button type="button">Reset</button><p>Baseline: 1 + 10 + 10×25 = 261 support occurrences per root.</p><output aria-live="polite"></output>';
 const controls=budget.querySelectorAll('input');function update(){let s=Number(controls[0].value),t=Number(controls[1].value);budget.querySelector('output').textContent=`Fanouts [${s},${t}]: 1 + ${s} + ${s}×${t} = ${1+s+s*t} occurrences/root; batch 512 = ${(512*(1+s+s*t)).toLocaleString('en-US')}. Unique IDs may be fewer.`;}
 controls.forEach(c=>c.addEventListener('input',update));budget.querySelector('button').onclick=()=>{controls[0].value=10;controls[1].value=25;update();};update();
})();
Teachback.mount(document.getElementById('teachback'),{prompt:'Explain the full path from a held-out protein to its 121 predictions, and why its features could not affect training.',points:['Training-only adjacency and scaler fit','Outward fanouts 10 then 25; inward aggregation','Shared first-layer weights; separate self and neighbor branches','Final normalization and multilabel head; validation selects rate'],model:'Fit preprocessing and weights using training tissues only. At inference, apply the fixed scaler to the new graph, collect 10 neighbors per root and 25 per neighbor, compute first-layer states with shared weights and then the second layer, normalize once, and apply the 121-label head. Validation chooses the learning rate; test labels score the selected model.'});
