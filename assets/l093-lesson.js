RetrievalBank.mount(document.getElementById('warmup'),{upTo:93,count:3});
HGTViz.mount(document.getElementById('attention-widget'));
Predict.mount(document.getElementById('prediction'),{prompt:'One author edge and one citation edge enter the same receiver/head. Which set shares a softmax denominator?',options:[{label:'Both incoming edges',value:'both'},{label:'Only author edges',value:'author'},{label:'Only citation edges',value:'citation'}],correct:'both',reveal:'HGT normalizes jointly over all incoming relation types for one receiver and head. Separate relation softmax would give two unit weights in this example.'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Trace an author→paper message, explain where relative time enters, and defend what the NN experiment establishes.',points:['Source K/V and target Q are type-specific.','Attention and message relation matrices are separate.','Softmax crosses incoming relation types.','Time augments the source before K/V.','Both target-label edge directions are removed.','NN MRR saturation and source discrepancies prevent a CS reproduction claim.'],model:'The source type chooses K and V, the target type chooses Q, and the edge type chooses attention and message transforms. After source-side time augmentation, each head normalizes compatibility scores jointly over the target’s incoming edges. Weighted messages are combined before a type-specific gated update. OAG task edges must be removed in both directions for seed papers. The NN subset has a universal field, so even a frequency baseline attains perfect MRR. The measured small NN comparison cannot establish the CS Table2 result or full-paper parity.'});
