RetrievalBank.mount(document.getElementById('warmup'),{upTo:80,count:3});
Teachback.mount(document.getElementById('teachback'),{prompt:'A frozen TabPFN checkpoint wins on random rows and loses on later rows. What can you conclude, and what must you test next?',points:['Distinguish prior fit from model capacity','Match the task, available features and tuning protocol','Random and temporal panels have different populations','Test point-in-time aggregates before attributing value to a GNN'],model:'This run supports a conditional comparison on these held-out panels. It does not isolate drift causally or reject ICL generally. I would inspect label and feature availability, the validation-selected recipe and subgroup changes, then compare point-in-time aggregates under the same temporal contract.'});
const boxes=[...document.querySelectorAll('#exam-gates input')];
function update(){const n=boxes.filter(x=>x.checked).length;document.querySelector('#exam-gates output').textContent=n===boxes.length?'Submission ready for tutor review. Mastery is not automatically graded.':`${n}/${boxes.length} evidence gates present. Complete the missing artifacts before submitting.`;}
boxes.forEach(x=>x.addEventListener('change',update));update();
