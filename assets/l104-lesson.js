RetrievalBank.mount(document.getElementById('l104-warmup'),{upTo:104,count:3});
AvailabilityAuditViz.mount(document.getElementById('l104-clock'));
Predict.mount(document.getElementById('l104-predict'),{prompt:'With trained weights fixed, must letting the model read future interactions increase AP?',options:[{label:'The score must always increase',value:'must'},{label:'The score can also decrease',value:'can'}],correct:'can',reveal:'Illegal access changes the information supplied to a fixed model. It can improve or damage rankings. The timestamp violation determines validity, not the sign of the metric change.'});
Teachback.mount(document.getElementById('l104-teachback'),{prompt:'Defend one corrected result. Trace a forbidden dependency and explain exactly which evidence your repair establishes.',points:['State the before-event or snapshot convention','Check event and availability times at every relevant request','Check label maturity and selection time separately','Pair events, negatives, weights and random draws','Distinguish replayed evaluation, reused training and course interventions'],model:'For before-event prediction I require event time strictly before the local query cutoff and availability no later than that cutoff. In recursive TGAT, an event at 6 is illegal for a child requested at 5 even when the root query is at 8. Fitting labels must mature before fitting, and model selection must use outcomes available before deployment. My AP comparison fixes the checkpoint and prediction questions. A passing timestamp audit does not certify unobserved arrival histories, and a ten-checkpoint evaluation reuses training rather than repeating the full paper.'});
