EventSnapshotViz.mountAggregation(document.getElementById('l105-aggregation'));
EventSnapshotViz.mountRelease(document.getElementById('l105-release'));
Predict.mount(document.getElementById('l105-predict'),{prompt:'After replacing binary snapshot edges with interaction counts, which claim is justified?',options:[{label:'Counts recover the original event order',value:'order'},{label:'Counts preserve totals but lose ordering',value:'totals'}],correct:'totals',reveal:'Two histories can have identical endpoint counts but opposite interaction order. A count is a sufficient summary for a total-count question, not for every temporal question.'});
Teachback.mount(document.getElementById('l105-teachback'),{prompt:'Choose a representation for a service that predicts at arbitrary times, then defend when a daily snapshot would be adequate.',points:['Specify the task and its prediction clock','Define window origin, width and endpoint convention','Explain what binary and weighted edges preserve','Enforce window release and late-arrival rules','Separate exact representation counts from predictive evidence'],model:'For an immediate event prediction I retain the timestamped stream and use only events strictly earlier than the query and available then. Daily weighted snapshots can answer completed-day interaction-count questions if within-day order is irrelevant. Windows are [start,end), fixed from origin 0, and the complete snapshot is usable only at max(end,latest constituent arrival). I cannot use the completed current day at noon. Counts retain multiplicity but not timing; these audit results do not establish an AP advantage for any model.'});
