/* L062 fixed two-context fixture: query2 value cannot enter q1. Optional self edge
 changes model to original-PFN access for comparison; no query targets are used. */
(()=>{const root=document.getElementById('v1-attention-viz');if(!root)return;
root.innerHTML='<p><strong>Predict:</strong> which value can change the existing query?</p><label>Second context value <input id="v1-context-value" type="range" min="0" max="10" step="1" value="2"> <output id="v1-context-readout">2</output></label><label>Unrelated query value <input id="v1-query-value" type="range" min="0" max="100" step="10" value="90"> <output id="v1-query-readout">90</output></label><label><input id="v1-self" type="checkbox"> Add q1 self edge (changes the architecture; self score 0, value 6)</label><button type="button" id="v1-reset">Reset fixture</button><pre id="v1-attention-readout" aria-live="polite"></pre>';
const c=root.querySelector('#v1-context-value'),q=root.querySelector('#v1-query-value'),s=root.querySelector('#v1-self');
function update(){const denom=1+Math.E+(s.checked?1:0),w0=1/denom,w1=Math.E/denom,wself=s.checked?1/denom:0;const value=w1*Number(c.value)+wself*6;root.querySelector('#v1-context-readout').value=c.value;root.querySelector('#v1-query-readout').value=q.value;root.querySelector('#v1-attention-readout').textContent=`Second value coordinate; scores held at [0,1].\nContext weights [${w0.toFixed(6)}, ${w1.toFixed(6)}]; q1 self weight ${wself.toFixed(6)}.\nBaseline v1: 0×.268941 + 2×.731059 = 1.462117.\nCurrent: 0×${w0.toFixed(6)} + ${c.value}×${w1.toFixed(6)} + 6×${wself.toFixed(6)} = ${value.toFixed(6)}.\nUnrelated query value ${q.value} is blocked in both architectures.\n${s.checked?'Self edge changes normalization and output. It uses features, not a hidden query target.':'Actual v1: own features remain through the residual; no self sending edge.'}`;root.dataset.output=String(value);}
[c,q,s].forEach(el=>el.addEventListener('input',update));root.querySelector('#v1-reset').addEventListener('click',()=>{c.value=2;q.value=90;s.checked=false;update()});update();})();
