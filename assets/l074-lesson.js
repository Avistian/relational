RetrievalBank.mount(document.getElementById('warmup'),{upTo:74,count:3});
CellGraph.mount(document.getElementById('cell-graph'));
Predict.mount(document.getElementById('prediction'),{prompt:'The encoder accepts a new schema. What follows about prediction quality?',options:[{label:'Transfer still needs measured evaluation',value:'measure'},{label:'Transfer always improves target prediction',value:'guarantee'}],correct:'measure',reveal:'A shared input representation makes the computation possible. Useful transfer depends on input semantics, task alignment, labels and optimization. Commit your expected pretrained-minus-scratch sign for each table before reading its results.'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Explain how a row with unfamiliar columns reaches a prediction. Separate representation compatibility, evidence of transfer, and relationships between database records.',points:['Cell values are nodes; column vectors condition messages.','Attention normalizes over the same receiver’s neighbors.','Frozen and fine-tuned controls answer different transfer questions.','Row graphs do not add foreign-key or temporal access guarantees.'],model:'Each observed cell supplies a value vector and column vector. Their product enters the key and value projections. Neighbor-normalized attention produces a center representation that a target head can read. Shared operations accept different schemas, but held-out matched comparisons determine whether transfer helps. Separate database records and time-valid joins require an additional relational graph and evaluation protocol.'});
