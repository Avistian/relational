RetrievalBank.mount(document.getElementById('l098-warmup'),{upTo:98,count:3});
Predict.mount(document.getElementById('l098-predict'),{prompt:'Freeze weights and request all neighbors for two layers. What should happen to seed logits?',options:[{label:'Match the full graph',value:'match'},{label:'Exceed the full graph',value:'exceed'},{label:'Ignore the full graph',value:'ignore'}],correct:'match',reveal:'The dependency closure contains the seed computation. Our 96 configurations match a dense oracle to within 9e-8; this says nothing about sequential optimizer-step equivalence.'});
Teachback.mount(document.getElementById('l098-teachback'),{prompt:'Why are a seed-only loss and a legal-time neighborhood separate requirements?',points:['Only query seeds contribute targets','Context nodes supply messages','Future features can leak without future labels','Different query times require distinct histories'],model:'The seed prefix decides which predictions are supervised. The time boundary decides which information those predictions may consume. A correct loss slice cannot repair future context; safe context cannot repair accidentally supervised context nodes.'});
