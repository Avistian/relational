RetrievalBank.mount(document.getElementById('warmup'),{upTo:107,count:3});
SnapshotStateViz.mount(document.getElementById('l107-node'),{mode:'node'});
SnapshotStateViz.mount(document.getElementById('l107-weight'),{mode:'weight'});
Predict.mount(document.getElementById('l107-predict'),{prompt:'Under a fixed mean-slope activation, change only the first of six snapshots. Which released variant can condition its final output on that change?',options:[{label:'Only H can respond',value:'h'},{label:'Only O can respond',value:'o'},{label:'Both variants can respond',value:'both'}],correct:'h',reveal:'H summarizes the earlier node features into its weight updates. Released O resets the same observation-free weight recurrence for each fixed-length query; earlier graph content has no path to its deterministic final output.'});
Teachback.mount(document.getElementById('l107-teachback'),{prompt:'Defend an hourly snapshot prediction at 09:05, and explain what object carries temporal information.',points:['Last usable window closes at 09:00 under a=t','Distinguish node-state rows from weight-matrix coordinates','Trace graph encoder, recurrence and pair head','Separate prediction, observation and gradient updates','Name a matched delay intervention and one reproduction limitation'],model:'At 09:05 I may use the completed 08:00–09:00 interaction graph. A GCN mixes neighbors inside that graph; a node GRU carries an identity-aligned N-by-d state into later predictions. EvolveGCN instead evolves feature-to-feature weights. I score candidate pairs before their observations update history. I would hold a trained encoder, questions and candidates fixed while delaying its legal history to the same window boundary, then measure the score change. That isolates an access intervention more narrowly than swapping entire model families. Source parity checks an implementation; modern runtime and released O versus paper LSTM still prevent a historical-identity claim.'});
