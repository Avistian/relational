RetrievalBank.mount(document.getElementById('warmup'),{upTo:92,count:3});
MetapathViz.mount(document.getElementById('semantic'));
Predict.mount(document.getElementById('prediction'),{prompt:'If another node’s semantic score changes, which formulation can change this node’s path mixture?',options:[{label:'The global mixture',value:'global'},{label:'The local mixture',value:'local'},{label:'Both mixture forms',value:'both'}],correct:'global',reveal:'Holding this node’s embeddings fixed, paper-global scoring averages scores over nodes before softmax. The released per-node scorer uses only this node’s two path representations.'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Explain PAP reachability, the two attention axes, the paper/release discrepancy, and what the ACM experiment establishes.',points:['Distinct endpoints differ from walk counts.','Node softmax runs over one receiver’s eligible senders.','Semantic softmax runs over paths.','Paper averages scores before softmax; the release does not.','KNN uses frozen test-node embeddings and new probe splits.','Historical and full-paper parity remain unestablished.'],model:'PAP connects papers sharing an author, collapsing duplicate paths into one endpoint edge. Each path has a GAT branch whose softmax selects among eligible neighbors. Semantic attention then mixes path representations. The paper shares a softmax of node-averaged scores; the release uses per-node softmax. Our full ACM release reconstruction evaluates a separately fitted KNN on frozen embeddings, so its score is not the neural head’s accuracy. Modern runtime, mirror provenance, missing historical seeds and paper/code differences prevent a full-paper reproduction claim.'});
