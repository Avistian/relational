LinkPredictionViz.mountLeak(document.getElementById('edge-leak'));
LinkPredictionViz.mountRanking(document.getElementById('edge-rank'));
RetrievalBank.mount(document.getElementById('warmup'),{upTo:87,count:3});
Predict.mount(document.getElementById('prediction'),{prompt:'Do CN, AA and RA always rank candidate links identically?',options:[{label:'Their rankings always agree',value:'same'},{label:'Their rankings can differ',value:'different'}],correct:'different',reveal:'A high-degree common neighbor counts as one for CN, but gets less weight for AA and RA. The recorded full-data results show different AUCs.'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Why can an edge-level test mask still leak the target, and why must an MRR report name its candidates?',points:['Remove both message directions of each held-out relation.','Separate context from supervision.','State candidate eligibility, filtering, count and tie convention.','Static hidden-edge recovery is not future recommendation.'],model:'An encoder can observe a relationship even when its label is masked. I split undirected pairs before adding message directions and keep validation/test pairs out of context. MRR compares each positive with a chosen candidate set; changing that set changes the evaluation problem. Temporal recommendation additionally needs a time-valid history and eligibility rule.'});
