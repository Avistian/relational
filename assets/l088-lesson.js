RetrievalBank.mount(document.getElementById('warmup'),{upTo:88,count:3});
WLViz.mount(document.getElementById('wl-refinement'));
Predict.mount(document.getElementById('prediction'),{prompt:'Duplicate every node row; the original sum is nonzero. Which readout must change?',options:[{label:'The sum readout',value:'sum'},{label:'The mean readout',value:'mean'},{label:'The max readout',value:'max'}],correct:'sum',reveal:'The sum doubles. Mean retains the same proportions; max retains the same coordinate-wise presence. This is an information distinction, not a prediction of test accuracy.'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Why can GIN distinguish a path from a star yet fail on a six-cycle versus two triangles? Why does this not establish a ranking of test accuracy?',points:['Both comparisons start with identical node labels.','Sum retains neighbor counts; WL refines joint signatures.','The cycle and triangles stay 2-regular with matching color histograms.','Learned finite networks need not achieve the theoretical capacity.','Selected CV scores are not an untouched test.'],model:'A path and star have different degree multisets, so one injective refinement separates them. The cycle and triangles each have six equally labeled nodes with two equally labeled neighbors, at every round. Standard message passing cannot recover their connectivity difference from these states. Capacity only describes possible distinctions; optimization and generalization still need empirical evaluation with an appropriate selection boundary.'});
