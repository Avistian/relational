RetrievalBank.mount(document.getElementById('warmup'),{upTo:77,count:3});
Predict.mount(document.getElementById('prediction'),{prompt:'Two opposite histories have identical flat vectors and opposite labels. What can a deterministic classifier get correct?',options:[{label:'Exactly one per pair',value:'one'},{label:'Exactly two per pair',value:'two'}],correct:'one',reveal:'It makes one shared prediction for both vectors. One label agrees; the other disagrees.'});
FlattenLossViz.mount(document.getElementById('flatten-recall'),{});
CeilingViz.mount(document.getElementById('bound-viz'),'bound');
CeilingViz.mount(document.getElementById('repair-viz'),'repair');
CeilingViz.mount(document.getElementById('reach-viz'),'reach');
Teachback.mount(document.getElementById('teachback'),{prompt:'State the ceiling with its information boundary, then explain what the successful tabular repair proves.',points:['The flat vector excludes event order, IDs and row position.','Opposite labels share an identical vector.','The majority count in each collision class defines the bound.','The delta feature uses only eligible historical events.','A tabular classifier can succeed when relational information is supplied.','Real-data usefulness and cost remain empirical questions.'],model:'For the defined flat representation, each collision class is label-balanced. Any deterministic mapping must choose one shared prediction per class, giving accuracy at most one half. Eligible last-minus-first restores the discarded ordering and permits exact classification by a stump. This proves a limitation of the specified representation on the specified generator. It does not prove that graph models outperform tabular models on real tasks.'});
