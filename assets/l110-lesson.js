RetrievalBank.mount(document.getElementById('warmup'),{upTo:110,count:3});
TemporalCheckpointViz.mount(document.getElementById('l110-batches'));
Predict.mount(document.getElementById('l110-predict'),{prompt:'Before seeing results: what must a clean checkpoint policy guarantee?',options:[{label:'It must improve predictive accuracy',value:'accuracy'},{label:'It must restore consistent history',value:'state'}],correct:'state',reveal:'Consistent history is the guarantee. Accuracy may move either way; published-score agreement does not establish temporal validity.'});
Teachback.mount(document.getElementById('l110-teachback'),{prompt:'Defend a temporal GNN checkpoint: why can a strict-past sampler coexist with invalid state?',points:['The sampler controls adjacency access; memory is a second input path.','Timestamp ties cannot update each other under the strict convention.','Restore weights, memory, clocks and pending messages together.','Reset each evaluation branch from the selected post-validation state.','Event dates do not establish actual availability.'],model:'A strict sampler can exclude future edges while queued messages still carry equal-time information. Keep tied events together and restore a coherent selected checkpoint, including its pending messages. Both test branches start from that same state. These checks establish the declared event-time contract; actual ingestion histories are needed for availability claims.'});
