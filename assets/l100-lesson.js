RetrievalBank.mount(document.getElementById('l100-warmup'),{upTo:100,count:3});
Predict.mount(document.getElementById('l100-predict'),{prompt:'All-neighbor batches reproduce seed logits. Must sequential Adam updates match one full-batch update?',options:[{label:'Yes, matching logits suffices',value:'yes'},{label:'No, parameter updates intervene',value:'no'},{label:'Yes, identical seeds suffice',value:'seed'}],correct:'no',reveal:'Weighted accumulation at fixed parameters can recover the full gradient. Stepping Adam between batches changes both parameters and optimizer state; later gradients are evaluated at new weights.'});
Teachback.mount(document.getElementById('l100-teachback'),{prompt:'Your all-neighbor gradient audit passes. Explain what this proves, and which training and reproduction claims remain unproven.',points:['Typed identities and seed-only losses align','Batch means carry B/N weights at fixed parameters','Finite sampling and sequential updates change the computation','Course execution does not establish historical paper parity'],model:'The audit checks sampled seed predictions and weighted gradients against a full-graph computation at fixed weights. Finite fanouts and sequential optimizer steps require their own experiment. A complete ACM run establishes its declared protocol; full historical reproduction also requires aligned data, model, sampling, training, selection, seeds and metrics.'});
BatchWeighting.mount(document.getElementById('batch-weighting'));
