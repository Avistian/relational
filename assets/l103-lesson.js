RetrievalBank.mount(document.getElementById('l103-warmup'),{upTo:103,count:3});
TemporalAttentionViz.mount(document.getElementById('l103-time'));
Predict.mount(document.getElementById('l103-predict'),{prompt:'With fixed cosine keys and fixed values, must the older event always receive less attention as query time increases?',options:[{label:'Its weight must always decrease',value:'decrease'},{label:'Its weight can later increase',value:'increase'}],correct:'increase',reveal:'Cosine features are periodic. Attention is a learned function of time and content, not a built-in monotone recency decay. Move the query-time slider and trace both contributions.'});
Teachback.mount(document.getElementById('l103-teachback'),{prompt:'Explain TGAT to a colleague who understands TGN. Trace a two-hop time boundary, the role of time in attention, and the limits of the Wikipedia evidence.',points:['No persistent recurrent node state; timestamped history is still required','Each recursive child receives the interaction time as its cutoff','Time features enter both keys and values','Paired Fourier identity differs from the released cosine-only encoder','Release replay, corrected comparison and full-paper coverage are distinct'],model:'TGAT retrieves historical interactions and recomputes a node embedding at the requested time. A child reached through an event at time 5 must use history strictly before 5, even if the root query is at time 8. Learned time features influence both attention compatibility and the values being mixed. The paper motivates this through paired Fourier features; its released encoder uses learned cosines and phases. A source-checked Wikipedia replay and a small matched comparison support bounded implementation and experimental claims, not all paper results or general model superiority.'});
