RetrievalBank.mount(document.getElementById('warmup'),{upTo:76,count:3});
Predict.mount(document.getElementById('prediction'),{prompt:'One event has [4,3]. Two events have [2,1] and [6,5]. Can their means distinguish the histories?',options:[{label:'Both means are equal',value:'equal'},{label:'Both means are different',value:'different'}],correct:'equal',reveal:'Both means are [4,3]. A count or sum can distinguish these particular histories.'});
RDLStackViz.mount(document.getElementById('time-viz'),'time');
RDLStackViz.mount(document.getElementById('aggregate-viz'),'aggregate');
Teachback.mount(document.getElementById('teachback'),{prompt:'Trace a prediction from raw tables to loss. Explain how identity, time and gradients constrain every boundary.',points:['Table-specific encoders produce row vectors; keys remain metadata.','FK values map to current PK positions.','Only events available by the query cutoff can contribute; fitted state also needs protection.','Mean pools repeated destinations and defines empty groups as zero.','Concatenation and the head produce logits; gradients reach both encoders.','Fixture loss is not benchmark evidence.'],model:'Fit allowed feature state, encode each table and retain the row-to-ID mapping. Map foreign keys to receiver positions and filter by event and availability time. Reduce eligible event vectors per customer, preserving zero-neighbor behavior. Concatenate own-row and neighbor vectors, predict logits and compute the target loss. Differentiable operations send gradients to both encoders. Verify identity permutations, future interventions and gradient support before interpreting any score.'});
