RetrievalBank.mount(document.getElementById('warmup'),{upTo:78,count:3});
Predict.mount(document.getElementById('prediction'),{prompt:'C changes from 8 to 20. What happens to A after exactly one synchronous round?',options:[{label:'A stays at three',value:'same'},{label:'A rises to six',value:'rise'},{label:'A drops to zero',value:'drop'}],correct:'same',reveal:'A reads only old B=4. Its first update stays 3. C affects A after a second round.'});
MessagePassingViz.mount(document.getElementById('message-viz'));
MessagePassingViz.mount(document.getElementById('reach-viz'),{mode:'reach'});
MessagePassingViz.mount(document.getElementById('normalization-viz'),{mode:'normalization'});
Teachback.mount(document.getElementById('teachback'),{prompt:'Explain why a GCN is neither an ordinary neighbor mean nor automatically a point-in-time-safe database model.',points:['Both endpoint degrees determine GCN weights','Self-loops are included before computing degrees','Every hop, feature and normalization graph needs availability filtering','Cora is transductive; test labels remain outside fitting'],model:'A GCN sums self and neighbor messages weighted by the inverse square root of both augmented degrees; weights need not sum to one. A graph layer knows nothing about availability time. Construct every hop and its degree normalization from eligible information. Cora permits all node features and graph structure during fitting, but only training labels enter gradients.'});
